# TASKS

## Summary

- source: `docs/SPEC.md` (기존 로또 기능의 회귀 기준)
- feature_source: `docs/feature/wheel-draw/DESIGN.md` (구조 변경과 돌림판 기능 기준)
- prd: `docs/feature/wheel-draw/PRD.md`
- last_updated: 2026-08-14
- overall_status: `done`
- progress: 14/14 tasks done
- current_phase: 완료
- next_task: 없음
- open_questions: 없음
- source_scope:
  - `docs/SPEC.md`의 비복원 추첨, 로또 세션, 2D·3D 렌더링 요구사항은 `features/lottery`에만 적용한다.
  - 돌림판의 신규 요구사항은 T1에서 작성할 feature `SPEC.md`와 현재 `DESIGN.md`를 우선한다.
  - 미확정 질문에 답이 필요한 구현은 추측하지 않고 T1 완료 후 진행한다.
  - 추첨기 선택 셸 추가를 제외한 기존 로또 기능의 규칙·상태·UI·저장·효과음·결과 동작과 MCP 경계는 변경하지 않는다.

## Execution Order

1. 요구사항·경계 확정: T1, T2
2. 기존 로또 수직 격리: T3
3. 웹 셸과 공통 코어 정리: T4, T5
4. 돌림판 독립 모듈 구현: T6, T7, T8
5. 돌림판 UI와 흐름 연결: T9, T10
6. 반응형·접근성·시각 검수: T11
7. 전체 통합·회귀 검증: T12
8. 공통 문서 최신화: T13

T1과 T2는 서로 독립적으로 진행할 수 있다. T6·T7·T8은 T1에서 확정한 제품 계약을 공유하되 구현 책임은 분리한다. 구조 이동과 신규 기능 구현을 같은 TASK에서 섞지 않는다.

## Tasks

- [x] T1 | done | 돌림판 1차 제품 요구사항과 완료 계약 확정
  - scope: `DESIGN.md`의 미확정 항목을 결정하고 `docs/feature/wheel-draw/PRD.md`와 `SPEC.md`에 돌림판 입력·회전·결과·저장·UI·오류·검수 요구사항을 기록한다.
  - acceptance:
    - 복원 추첨 기본 정책과 당첨 후보 제거 옵션 여부가 명시되어 있다.
    - 회전 횟수의 기본값·최댓값·후보 수와의 관계가 명시되어 있다.
    - 수동·자동 모드의 1차 범위와 완료 조건이 명시되어 있다.
    - 회전 시간, 중복 조작 방지, 재회전·설정 복귀 의미가 명시되어 있다.
    - 후보 입력 제한, 구획 레이블, 결과 이력과 저장 범위가 명시되어 있다.
    - 효과음, 결과 복사·이미지 저장, 직접 URL과 뒤로 가기의 범위가 명시되어 있다.
    - 오류·접근성·모바일·브라우저 검수 조건이 테스트 가능한 문장으로 정의되어 있다.
    - 기존 로또 SPEC과 다른 요구사항이 돌림판에만 적용됨을 명시한다.
    - 추첨기 선택 셸 외의 기존 로또 동작에 영향을 주지 않는 비회귀 요구사항이 최상위 수용 기준으로 명시되어 있다.
  - dependencies: 없음
  - progress: 요구사항 인터뷰를 종료하고 복원·수동·무제한 회전, 약 4초 자연스러운 감속, 후보·설정 저장, 결과 이력 복사·비우기, 효과음, 선택 내비게이션과 로또 비회귀 계약을 PRD·SPEC·DESIGN에 반영했다.
  - next_action: 사용자에게 T1 산출물 피드백을 받은 뒤 T2를 시작한다.
  - notes: `PRD.md`, `SPEC.md` 신규 작성, `DESIGN.md` 상태 approved 및 확정 요구사항 반영

- [x] T2 | done | 추첨기 기능 간 소스 경계 자동 검증 추가
  - scope: 기존 `verify-boundaries.mjs`에 웹 셸·공유 계층·로또·돌림판의 허용 의존 방향을 검사하는 규칙과 회귀 테스트를 추가한다.
  - acceptance:
    - `features/lottery`와 `features/wheel`의 상호 import를 실패로 검출한다.
    - `core`와 `web/shared`가 특정 추첨기 기능을 import하면 실패한다.
    - 웹 셸이 기능 내부 도메인·서비스를 직접 import하면 실패하고 공개 진입점만 허용한다.
    - 기존 web·MCP App·MCP·API 경계와 `Math.random()` 금지 검사가 유지된다.
    - 기능 디렉터리가 아직 없거나 이동 중인 단계에서도 경계 검증 도구가 명확하게 동작한다.
    - 허용·금지 import 사례가 자동 테스트 또는 검증 fixture로 확인된다.
  - dependencies: 없음
  - progress: 웹 셸·shared·기능 경계 판정을 순수 모듈로 분리하고 6개 Node fixture 테스트를 추가해 실제 저장소 검증기에 연결했다.
  - next_action: 완료. T3 구조 이동에서 실제 경계 규칙이 통과하는지 계속 검증한다.
  - notes: `npm run test:boundaries`, `npm run verify:boundaries` 통과

- [x] T3 | done | 기존 로또 웹 기능을 수직 기능 디렉터리로 이동
  - scope: 현재 웹의 로또 도메인·컴포넌트·서비스·테스트와 로또 전용 App 조정 코드를 `src/web/features/lottery/`로 이동하고 공개 진입점을 만든다.
  - acceptance:
    - 로또 세션, 일정, 2D·3D 렌더러, 효과음, 저장소와 결과 기능이 `features/lottery` 안에 위치한다.
    - `src/web/App.tsx`에서 로또 세션 내부 상태와 서비스 조정을 제거하고 로또 공개 진입점 뒤로 숨긴다.
    - 기존 `lottery-draw:names:v1`, `lottery-draw:setup-options:v1` 키와 복원 동작이 유지된다.
    - 기존 수동·자동 추첨, 재추첨, 백그라운드 복구, 2D·3D 전환, 복사·이미지 저장 동작이 바뀌지 않는다.
    - 기존 웹 테스트는 의미를 유지한 채 새 위치에서 통과한다.
    - 파일 이동 과정에서 로또 내부 타입과 렌더러를 불필요하게 범용화하지 않는다.
    - T2의 기능 경계 검증이 통과한다.
  - dependencies: T2
  - progress: 기존 App·domain·components·services·tests·CSS를 `features/lottery`로 이동하고 루트 App이 공개 진입점만 마운트하도록 변경했다.
  - next_action: 완료. T4에서 루트 App을 선택 셸로 확장한다.
  - notes: 웹 테스트 71개, `build:web`, `verify:boundaries` 통과. 기존 저장 키와 기능 로직 변경 없음

- [x] T4 | done | 추첨기 선택 웹 셸과 선택 저장소 구현
  - scope: 로또와 돌림판을 선택·마운트하는 웹 셸, 표시용 메타데이터와 셸 전용 저장소를 구현한다.
  - acceptance:
    - 셸은 `lottery`와 `wheel`의 타입·라벨·설명 및 공개 컴포넌트만 안다.
    - 셸이 선택된 기능의 세션, 후보, 결과와 설정을 읽거나 변경하지 않는다.
    - 손상되거나 없는 선택 저장값은 기존 사용자 호환 기본값으로 복구된다.
    - `roulette:selected-experience:v1` 이외의 기능 저장값을 셸이 수정하지 않는다.
    - T1에서 확정한 선택 화면, 기능 전환, 직접 URL과 뒤로 가기 계약을 만족한다.
    - 로또와 돌림판의 기능 로딩 실패가 최상위 오류 경계에서 격리된다.
    - 셸 선택과 저장값 복원 흐름의 UI 테스트가 있다.
  - dependencies: T1, T3
  - progress: hash 선택 셸, 마지막 선택 저장소, 기능 오류 경계와 돌림판 공개 진입점 placeholder를 구현했다.
  - next_action: 완료. T5에서 돌림판이 재사용할 공통 코어 경계를 점검한다.
  - notes: 웹 테스트 15파일·82개, `build:web`, `verify:boundaries`, `git diff --check` 통과. 셸은 기능 공개 진입점과 표시 메타데이터만 참조한다.

- [x] T5 | done | 공통 후보·입력·난수 프리미티브 경계 정리
  - scope: 현재 `core`에서 돌림판도 안전하게 재사용할 수 있는 후보 타입, 단일 인덱스 난수와 입력 문법을 확인하고 필요한 최소 조정만 수행한다.
  - acceptance:
    - 돌림판은 `secureRandomIndex`를 사용하고 `drawCandidates`의 비복원 결과 계약에 의존하지 않는다.
    - `drawCandidates`와 `createDrawOrder`는 로또·MCP가 선택적으로 사용하는 비복원 알고리즘으로 유지된다.
    - T1에서 로또와 다른 후보 제한이 확정된 경우에만 문법 파싱과 제품별 개수·길이 검증을 분리한다.
    - 후보 정책이 동일하면 불필요한 파서 계층을 추가하지 않고 기존 파서를 재사용한다.
    - `core`에는 세션, 렌더링, 저장소 또는 추첨기 capability가 추가되지 않는다.
    - 기존 core·MCP 입력과 추첨 테스트가 모두 통과한다.
  - dependencies: T1
  - progress: 돌림판과 로또의 후보 문법·제한이 동일하고 `secureRandomIndex`가 독립 프리미티브로 이미 제공됨을 확인해 공통 코어는 변경하지 않았다.
  - next_action: 완료. T6에서 `secureRandomIndex`만 사용하는 돌림판 복원 추첨 세션을 기능 내부에 구현한다.
  - notes: `drawCandidates`와 `createDrawOrder`는 기존 비복원 계약으로 유지한다. core 테스트 3파일·20개, MCP 테스트 3파일·17개와 `build:mcp` 통과

- [x] T6 | done | 돌림판 복원 추첨 세션 엔진 구현
  - scope: `features/wheel/domain`에 후보, 활성 회전, 고유 결과 사건과 오류 상태를 관리하는 순수 세션 전이를 구현한다.
  - acceptance:
    - 회전 시작 시 Web Crypto 기반 인덱스로 목표 후보를 먼저 확정한다.
    - 당첨 후보를 후보 목록에서 제거하지 않고 같은 후보의 연속 당첨을 허용한다.
    - 각 결과는 후보 ID와 별도의 고유 outcome ID 및 회전 순서를 가진다.
    - 활성 회전 중 추가 시작 요청은 상태를 변경하지 않는다.
    - 회전 완료 이벤트가 중복되어도 결과는 한 번만 반영된다.
    - 각 회전 완료 후 다시 `ready` 상태가 되며 목표 횟수나 `completed` 상태 없이 반복 회전할 수 있다.
    - 난수 실패는 결과 없는 오류 상태로 전환되고 `Math.random()`으로 대체하지 않는다.
    - DOM, React, SVG, 타이머와 저장소에 의존하지 않는 단위 테스트가 있다.
  - dependencies: T1, T5
  - progress: `secureRandomIndex`로 목표를 선확정하고 복원 추첨, 고유 outcome, 중복 시작 방지, 멱등 완료, 오류 재시도와 결과 비우기를 순수 전이로 구현했다.
  - next_action: 완료. T7에서 후보 인덱스와 포인터 기준의 누적 목표 회전각을 계산한다.
  - notes: 웹 테스트 16파일·89개, `build:web`, `verify:boundaries`, `git diff --check` 통과

- [x] T7 | done | 돌림판 구획과 목표 각도 순수 계산 구현
  - scope: `features/wheel/domain/wheelGeometry`에 후보 구획, 포인터 보정과 최종 정지 각도 계산을 구현한다.
  - acceptance:
    - 후보 수에 따라 동일한 크기의 구획을 계산한다.
    - 후보 ID와 순서로 목표 구획 중심 각도를 구한다.
    - 현재 각도에서 T1의 최소 회전 수를 만족하는 최종 각도를 계산한다.
    - CSS/SVG 회전 방향과 포인터 기준 각도 보정이 한 모듈에 캡슐화된다.
    - 2개·45개 후보, 첫·마지막 구획과 360도 경계에서 목표 구획 안에 정지한다.
    - 같은 이름의 후보도 서로 다른 ID와 인덱스로 정확히 구분한다.
    - 모듈은 난수를 생성하거나 DOM·React에 의존하지 않는다.
  - dependencies: T1
  - progress: 상단 0도·시계 방향 좌표계에서 동일 구획, 중심각과 최소 6회 이상의 누적 목표 회전각을 순수 함수로 구현했다.
  - next_action: 완료. T8에서 공통 파서를 사용하는 돌림판 입력 모델과 전용 저장소를 구현한다.
  - notes: 2개·45개, 첫·마지막 구획, 0/360도 정렬과 연속 회전 테스트 포함. 웹 테스트 17파일·95개와 `build:web` 통과

- [x] T8 | done | 돌림판 설정·후보 입력·저장소 구현
  - scope: `features/wheel`이 소유하는 설정 모델, 후보 입력 검증과 버전 저장소를 구현한다.
  - acceptance:
    - T1에서 확정한 후보 수·이름 길이·회전 옵션을 검증하고 한국어 오류를 반환한다.
    - 후보 입력 문법은 T5에서 확정한 공통 경계를 통해 처리한다.
    - `wheel-draw:candidates:v1`, `wheel-draw:setup-options:v1`만 사용한다.
    - 저장값이 없거나 손상된 경우 돌림판 기본값과 경고로 복구한다.
    - 돌림판 저장 실패가 현재 입력·회전 기능과 로또 저장값에 영향을 주지 않는다.
    - 진행 중 세션과 결과 이력은 저장하지 않는다.
    - 저장·복원·삭제·손상 데이터와 기능 간 키 격리 테스트가 있다.
  - dependencies: T1, T5
  - progress: 공통 `parseNames` 결과를 고유 구획 ID로 변환하고 후보 원문·효과음 설정을 별도 v1 키에 저장·복원·삭제하도록 구현했다.
  - next_action: 완료. T9에서 T7 각도를 표현하는 SVG 구획과 동작 감소 회전을 구현한다.
  - notes: 후보 2·45·46개, 이름 20·21자, 중복 이름, 저장 손상·실패·키 격리 테스트 포함. 웹 테스트 19파일·104개, `build:web`, `verify:boundaries` 통과

- [x] T9 | done | SVG 돌림판 렌더러와 회전 연출 구현
  - scope: `WheelStage`에서 후보 구획, 레이블, 포인터와 확정 목표까지의 회전 애니메이션을 표시한다.
  - acceptance:
    - 렌더러는 세션이 제공한 목표 후보와 T7의 각도 계산만 사용하며 결과를 선택하지 않는다.
    - 후보 구획과 포인터가 2~45개 후보에서 정상 렌더링된다.
    - 중복 이름 후보는 서로 다른 구획으로 유지된다.
    - 긴 이름은 T1의 축약 규칙을 적용하고 전체 이름을 확인할 수 있는 보조 표현을 제공한다.
    - 단일 transform 기반 회전으로 프레임별 DOM 재구성을 피한다.
    - 회전 중 리사이즈와 저성능 프레임에서도 최종 목표 구획이 바뀌지 않는다.
    - `prefers-reduced-motion` 환경은 T1에서 확정한 짧은 전환 또는 즉시 결과 표시를 사용한다.
    - 렌더링 실패는 추첨 결과를 변경하지 않고 기능 UI에 안전한 오류로 전달된다.
  - dependencies: T1, T7
  - progress: 2~45개 SVG 구획·축약 레이블·고정 포인터와 누적 목표각을 표현하는 단일 그룹 애니메이션을 구현했다.
  - next_action: 완료. T10에서 세션의 목표와 논리적 공개 시각을 렌더러·제어·결과에 연결한다.
  - notes: 일반 4초 다단계 가속·감속, 동작 감소 220ms 정규화 회전, 애니메이션 실패 시 최종 transform 유지. 웹 테스트 20파일·108개와 `build:web` 통과

- [x] T10 | done | 돌림판 설정·회전 제어·결과 이력 통합
  - scope: `WheelApp`, `WheelSetup`, `WheelControls`, `WheelResultHistory`를 세션 엔진·저장소·렌더러와 연결한다.
  - acceptance:
    - 유효한 후보와 설정으로 돌림판 세션을 시작할 수 있다.
    - 회전 시작 시 목표가 확정되고 연출 완료 후 동일한 결과가 한 번 누적된다.
    - 빠른 버튼 연타, 타이머 중복과 컴포넌트 재렌더링이 중복 결과를 만들지 않는다.
    - 반복 당첨 결과가 고유 outcome ID를 React 키로 사용해 모두 표시된다.
    - 수동 반복 회전, 결과 비우기, 처음부터 다시와 추첨기 선택 복귀 흐름이 동작한다.
    - 설정 복귀·기능 전환·언마운트 시 진행 중 타이머와 애니메이션을 정리한다.
    - 결과 복사와 효과음은 돌림판 내부 서비스로 구현하고 실패를 격리하며 이미지 저장은 추가하지 않는다.
    - 입력부터 반복 당첨·결과 비우기까지 가짜 시계를 사용한 통합 테스트가 있다.
  - dependencies: T4, T6, T8, T9
  - progress: 설정, 절대 `revealAt` 기반 반복 회전, 결과 복사·비우기, 초기화, 효과음과 언마운트 정리를 독립 `WheelApp`에 통합했다.
  - next_action: 완료. T11에서 데스크톱·모바일·키보드·동작 감소 흐름을 실제 브라우저로 검수하고 미세 조정한다.
  - notes: 웹 테스트 22파일·117개, `build:web`, `verify:boundaries`, `git diff --check` 통과

- [x] T11 | done | 추첨기 선택과 돌림판 반응형·접근성·시각 마감
  - scope: 기존 제품의 시각 방향을 유지하면서 선택 화면과 돌림판의 데스크톱·모바일 레이아웃 및 접근 가능한 상태 표현을 완성한다.
  - acceptance:
    - 로또와 돌림판 선택의 차이와 현재 위치가 텍스트로도 구분된다.
    - 키보드만으로 추첨기 선택, 설정, 회전과 설정 복귀가 가능하다.
    - 회전 상태와 최신 결과를 적절한 레이블 또는 live region으로 확인할 수 있다.
    - 320px 이상 모바일과 데스크톱에서 돌림판·포인터·제어·결과가 잘리거나 겹치지 않는다.
    - 45개 구획에서도 전체 이름 결과와 회전판 축약 이름의 역할이 구분된다.
    - 사용자 입력은 SVG/DOM에서 HTML이 아닌 텍스트로만 렌더링된다.
    - 로또 전용 스타일과 돌림판 스타일이 기능 디렉터리에서 격리되고 공통 셸 스타일만 상위에 남는다.
    - T1에서 확정한 브라우저와 동작 감소 환경의 수동 검수가 완료된다.
  - dependencies: T10
  - progress: 선택·설정·회전·후보·결과를 320px과 데스크톱에서 검수하고, 45개 번호 밀도와 셸 복귀 버튼의 로또 브랜드 겹침을 수정했다.
  - next_action: 완료. T12에서 전체 자동 검증과 출시 전 교차 브라우저 스모크 범위를 최종 확인한다.
  - notes: Chrome과 인앱 Chromium에서 선택·로또·돌림판, 8개·45개 후보, 4초 회전·목표 중심 정지, 320px 무가로넘침과 콘솔 오류 없음을 확인했다. 키보드 선택·동작 감소 220ms는 자동 테스트로 검증했다. 현재 연결 환경에 없는 Safari·Firefox·Edge 실기 검수는 T12 출시 검증의 잔여 항목으로 기록한다. 웹 테스트 22파일·118개, `build:web`, `verify:boundaries` 통과

- [x] T12 | done | 전체 통합·회귀·빌드 경계 검증
  - scope: 추첨기 선택부터 두 기능의 핵심 흐름, 기존 MCP와 프로덕션 빌드를 함께 검증하고 발견한 회귀를 수정한다.
  - acceptance:
    - 선택 화면에서 로또와 돌림판을 각각 시작하고 다시 전환할 수 있다.
    - 로또의 기존 SPEC 자동 테스트와 핵심 브라우저 흐름이 회귀하지 않는다.
    - 돌림판의 복원 추첨, 반복 outcome, 중복 조작 방지와 목표 각도가 자동 테스트를 통과한다.
    - 기능 전환 후에도 각 기능의 입력·옵션 저장값이 서로 유지되고 오염되지 않는다.
    - `npm run test:core`, `npm run test:web`, `npm run test:mcp-app`, `npm run test:mcp`가 통과한다.
    - `npm run build:web`, `npm run build:mcp`, `npm run verify:boundaries`가 통과한다.
    - 모바일·데스크톱에서 선택·로또·돌림판 핵심 흐름의 브라우저 스모크 테스트가 완료된다.
    - 추첨 정확성, 중복 반영, 저장값 유실과 기능 간 import 경계에 알려진 결함이 없다.
  - dependencies: T3, T4, T5, T6, T7, T8, T9, T10, T11
  - progress: 기능 전환 시 로또·돌림판 입력과 옵션을 독립적으로 복원하는 통합 테스트를 추가하고 전체 자동 검증을 완료했다.
  - next_action: 완료. T13에서 구현된 구조와 검증 결과를 공통·기능 문서에 반영한다.
  - notes: core 20개, web 119개, MCP App 18개, MCP 17개와 웹 기능 경계 fixture 6개 통과. 웹·MCP 빌드, 저장값 격리, Chrome·인앱 Chromium 데스크톱·320px 스모크 확인. Safari·Firefox·Edge 실기 검수는 현재 실행 환경에서 지원되지 않는다.

- [x] T13 | done | 구현 결과에 맞춘 공통 문서와 작업 상태 최신화
  - scope: 완료된 코드와 검증 결과를 기준으로 README, 공통 아키텍처·개발 문서와 현재 feature 문서를 최신화한다.
  - acceptance:
    - `README.md`가 로또와 돌림판 선택, 기능 차이와 실행 방법을 설명한다.
    - `docs/architecture.md`가 웹 셸·기능 수직 격리·공유 코어·MCP 경계를 반영한다.
    - `docs/development.md`가 기능 추가 위치, import 경계, 목적별 테스트와 디버깅 방법을 설명한다.
    - 현재 feature `SPEC.md`, `DESIGN.md`, `TASK.md`가 최종 구현과 일치한다.
    - TASK별 상태, 진행 결과, 검증 명령과 알려진 제한이 재개 가능한 수준으로 기록된다.
    - 문서의 경로·명령·저장 키와 타입명이 실제 코드와 일치한다.
  - dependencies: T12
  - progress: README에 두 추첨기 차이와 실행 흐름을 추가하고, 공통 아키텍처·개발 가이드와 feature SPEC·DESIGN을 실제 수직 기능 구조·명령·저장 키·검증 결과에 맞췄다.
  - next_action: 완료. 후속 배포 또는 지원 브라우저 실기 검수는 별도 작업으로 진행한다.
  - notes: `docs/architecture.md`, `docs/development.md`의 revision을 작업 기준 HEAD `94d57b666972420f324eb51b0445223bb9564482`로 갱신했다. 현재 환경에서 수행하지 못한 Safari·Firefox·Edge 실기 검수 제한을 SPEC·TASK에 유지했다.

- [x] T14 | done | 회전 강도에 따른 시간·회전량 가변화
  - scope: 고정 4초·6회 회전을 회전 강도에 따라 함께 변하는 자연스러운 연출로 변경한다.
  - acceptance:
    - 일반 회전은 6~10회의 전체 회전과 3.8~5.2초 범위를 사용한다.
    - 약한 회전에서 강한 회전으로 갈수록 회전량과 시간이 함께 증가한다.
    - 연출 강도 선택은 당첨 후보 선택과 분리하고 Web Crypto 기반 난수를 사용한다.
    - 연출 난수만 실패하면 최소 프로필로 복구하되 당첨 난수 실패 계약은 유지한다.
    - 동작 감소 환경은 기존 220ms 전환과 동일 결과 계약을 유지한다.
    - 최소·최대 강도에서 회전각, 지속 시간과 결과 공개 시각을 자동 테스트한다.
  - dependencies: T13
  - progress: 6~10회와 3.8~5.2초를 선형 연동하는 회전 프로필, 렌더러 지속 시간 주입과 연출 난수 실패 최소 프로필 복구를 구현했다.
  - next_action: 완료. 후속 회전 감각 조정은 같은 계약 범위 안에서 브라우저 검수 후 진행한다.
  - notes: 최소·최대 프로필과 결과 공개 시각, 연출 난수 실패 격리 테스트를 추가했다. `npm run verify`, `git diff --check` 통과. core 20개, web 122개, MCP App 18개, MCP 17개, 경계 fixture 6개

## Scope Guard

- 현재 계획은 정적 웹앱의 구조 변경과 돌림판 기능 추가만 포함한다.
- Remote MCP 도구에 추첨기 종류 선택을 추가하지 않는다.
- 서버 저장소, 인증, 결과 동기화, 분석과 감사 로그를 추가하지 않는다.
- 새 범용 세션 프레임워크나 추첨기 capability 시스템을 만들지 않는다.
- 별도 웹 빌드·배포는 DESIGN의 재검토 조건이 실제로 충족되기 전까지 도입하지 않는다.
- 미확정 제품 요구사항을 구현 편의에 따라 추측하지 않는다.

## 진행 로그

- 2026-08-14 16:54 | 단계: T14 | 상태: not_started -> in_progress
  - 요약: 고정 회전 계약을 6~10회·3.8~5.2초의 강도 연동 프로필로 변경하기 시작했다.
  - 산출물: `WheelApp`, `WheelStage`와 관련 테스트 변경 진행 중
  - 검증: 기존 결과 선확정·동작 감소·최소 6회 계약 유지 여부 확인
  - 리스크_또는_차단: 회전 연출 난수가 당첨 후보 분포나 난수 실패 처리에 영향을 주면 안 된다.
  - 다음: 최소·최대 프로필 테스트와 전체 검증
  - 사용자_피드백: 반영 완료(최대 10바퀴 허용, 계약·구조 변경은 TASK 기록 요청)

- 2026-08-14 16:56 | 단계: T14 | 상태: in_progress -> done
  - 요약: 회전 강도에 따라 6~10바퀴와 3.8~5.2초가 함께 증가하도록 구현하고 계약 문서를 동기화했다.
  - 산출물: `WheelApp.tsx`, `WheelStage.tsx`, 관련 테스트, README·공통·feature 계약 문서
  - 검증: `npm run verify`, `git diff --check` 통과. web 22파일·122개 포함 전체 테스트·빌드·경계 검증 성공
  - 리스크_또는_차단: Safari·Firefox·Edge 실기 감각 검수는 현재 실행 환경 제한으로 남아 있다.
  - 다음: 없음
  - 사용자_피드백: 반영 완료(최대 10바퀴와 TASK 기록 기준)

- 2026-08-14 16:28 | 단계: T12 | 상태: not_started -> in_progress
  - 요약: 전체 테스트·빌드·경계 검증과 기능 전환 저장값 격리 확인을 시작했다.
  - 산출물: 검증 및 필요한 회귀 테스트 예정
  - 검증: T11 브라우저 스모크 결과와 T12 수용 기준 확인
  - 리스크_또는_차단: 현재 실행 환경에서 Safari·Firefox·Edge 실기 검수는 지원되지 않는다.
  - 다음: `npm run verify`와 저장값 격리 통합 테스트 실행
  - 사용자_피드백: 반영 완료(잔여 작업을 중간 피드백 없이 연속 진행 승인)

- 2026-08-14 16:29 | 단계: T12 | 상태: in_progress -> done
  - 요약: 두 기능의 저장값 격리 회귀 테스트를 추가하고 전체 통합·빌드·경계 검증을 완료했다.
  - 산출물: `App.test.tsx` 기능 전환 저장값 격리 통합 테스트
  - 검증: `npm run verify`, `git diff --check` 통과. core 20개, web 119개, MCP App 18개, MCP 17개, 경계 fixture 6개
  - 리스크_또는_차단: Safari·Firefox·Edge 실기 검수는 현재 실행 환경에서 지원되지 않으며 Chrome 계열 스모크와 표준 기반 자동 테스트로 대체했다.
  - 다음: T13 공통·feature 문서 최신화
  - 사용자_피드백: 반영 완료(잔여 작업 연속 진행)

- 2026-08-14 16:29 | 단계: T13 | 상태: not_started -> in_progress
  - 요약: 실제 구현 구조와 검증 명령을 기준으로 공통·feature 문서 동기화를 시작했다.
  - 산출물: 문서 변경 예정
  - 검증: T12 전체 검증 결과를 문서 기준으로 사용
  - 리스크_또는_차단: 없음
  - 다음: README·architecture·development·feature 문서 대조 및 갱신
  - 사용자_피드백: 반영 완료(잔여 작업 연속 진행)

- 2026-08-14 16:33 | 단계: T13 | 상태: in_progress -> done
  - 요약: 구현 결과와 검증 근거를 README, 공통 아키텍처·개발 가이드와 feature 문서에 동기화했다.
  - 산출물: `README.md`, `docs/architecture.md`, `docs/development.md`, `SPEC.md`, `DESIGN.md`, `TASK.md`
  - 검증: 실제 소스 트리·상수·저장 키·package script 대조, `git diff --check` 통과
  - 리스크_또는_차단: 알려진 기능 결함 없음. Safari·Firefox·Edge 실기 검수는 현재 실행 환경 제한으로 별도 기록
  - 다음: 없음
  - 사용자_피드백: 반영 완료(잔여 작업 무피드백 연속 진행 완료)

- 2026-08-14 15:58 | 단계: T6 | 상태: not_started -> in_progress
  - 요약: DOM·React·타이머와 분리된 돌림판 복원 추첨 세션 구현을 시작했다.
  - 산출물: 구현 예정
  - 검증: T1·T5 의존성과 SPEC 세션 불변식 확인
  - 리스크_또는_차단: 활성 회전 중 중복 시작과 늦거나 중복된 완료 호출이 결과를 추가하지 않아야 한다.
  - 다음: 세션 전이 테스트와 순수 도메인 구현
  - 사용자_피드백: 반영 완료(T6부터 T11까지 중간 피드백 없이 연속 진행 승인)

- 2026-08-14 16:00 | 단계: T6 | 상태: in_progress -> done
  - 요약: 결과를 회전 전에 확정하고 같은 후보의 반복 당첨을 고유 사건으로 누적하는 순수 세션을 구현했다.
  - 산출물: `wheelSession.ts`, 상태 전이 단위 테스트 7개
  - 검증: 웹 테스트 16파일·89개, `npm run build:web`, `npm run verify:boundaries`, `git diff --check` 통과
  - 리스크_또는_차단: UI 타이머는 `revealAt`을 기준으로 현재 outcome ID를 한 번만 완료해야 한다.
  - 다음: T7 돌림판 구획과 목표 각도 계산
  - 사용자_피드백: 반영 완료(T11까지 연속 진행)

- 2026-08-14 16:00 | 단계: T7 | 상태: not_started -> in_progress
  - 요약: DOM과 난수에서 독립적인 돌림판 좌표계와 누적 회전각 계산을 시작했다.
  - 산출물: 구현 예정
  - 검증: T1의 최소 6회 순방향 회전과 상단 포인터 계약 확인
  - 리스크_또는_차단: SVG 시계 방향 회전과 구획 중심 보정의 부호를 테스트로 고정해야 한다.
  - 다음: 2개·45개·0/360도 경계 테스트와 순수 함수 구현
  - 사용자_피드백: 반영 완료(T11까지 연속 진행)

- 2026-08-14 16:02 | 단계: T7 | 상태: in_progress -> done
  - 요약: 후보 구획 중심과 상단 포인터를 일치시키는 누적 시계 방향 회전각 계산을 구현했다.
  - 산출물: `wheelGeometry.ts`, 경계·연속 회전 단위 테스트 6개
  - 검증: 웹 테스트 17파일·95개, `npm run build:web`, `git diff --check` 통과
  - 리스크_또는_차단: T9 SVG 경로도 같은 상단 0도·시계 방향 좌표계를 사용해야 한다.
  - 다음: T8 돌림판 설정·입력·저장소
  - 사용자_피드백: 반영 완료(T11까지 연속 진행)

- 2026-08-14 16:02 | 단계: T8 | 상태: not_started -> in_progress
  - 요약: 공통 입력 문법을 재사용하면서 저장 키와 스키마는 돌림판 내부에 격리하는 작업을 시작했다.
  - 산출물: 구현 예정
  - 검증: 저장 계약의 후보·옵션 v1 스키마와 로또 키 비접근 조건 확인
  - 리스크_또는_차단: 저장 실패가 현재 메모리 입력과 효과음 선택을 되돌리지 않아야 한다.
  - 다음: 입력 경계 테스트와 메모리 Storage 기반 저장·복원·삭제 테스트 구현
  - 사용자_피드백: 반영 완료(T11까지 연속 진행)

- 2026-08-14 16:04 | 단계: T8 | 상태: in_progress -> done
  - 요약: 공통 파서를 직접 재사용하는 돌림판 입력 모델과 기능 전용 후보·옵션 저장소를 구현했다.
  - 산출물: `wheelSetup.ts`, `wheelStorage.ts`, 입력·저장소 단위 테스트 9개
  - 검증: 웹 테스트 19파일·104개, `npm run build:web`, `npm run verify:boundaries`, `git diff --check` 통과
  - 리스크_또는_차단: T10은 저장 실패 경고만 갱신하고 사용자가 입력한 메모리 값을 유지해야 한다.
  - 다음: T9 SVG 돌림판과 회전 연출
  - 사용자_피드백: 반영 완료(T11까지 연속 진행)

- 2026-08-14 16:04 | 단계: T9 | 상태: not_started -> in_progress
  - 요약: 목표 후보를 선택하지 않고 확정된 누적 회전각만 표현하는 SVG 렌더러 구현을 시작했다.
  - 산출물: 구현 예정
  - 검증: T7 상단 0도·시계 방향 좌표계 및 동작 감소 300ms 이하 계약 확인
  - 리스크_또는_차단: 브라우저 애니메이션 API 실패 시에도 최종 transform과 논리 결과가 유지되어야 한다.
  - 다음: 2~45개 정적 구획, 레이블, 포인터와 일반·동작 감소 애니메이션 테스트
  - 사용자_피드백: 반영 완료(T11까지 연속 진행)

- 2026-08-14 16:06 | 단계: T9 | 상태: in_progress -> done
  - 요약: 확정 목표각만 소비하는 SVG 돌림판과 일반·동작 감소 회전 표현을 구현했다.
  - 산출물: `WheelStage.tsx`, SVG·회전·실패 격리 컴포넌트 테스트 4개
  - 검증: 웹 테스트 20파일·108개, `npm run build:web`, `git diff --check` 통과
  - 리스크_또는_차단: 실제 결과 완료는 애니메이션 이벤트가 아니라 T10의 절대 `revealAt` 타이머가 담당해야 한다.
  - 다음: T10 설정·회전·결과 이력 통합
  - 사용자_피드백: 반영 완료(T11까지 연속 진행)

- 2026-08-14 16:06 | 단계: T10 | 상태: not_started -> in_progress
  - 요약: 돌림판 기능의 설정부터 반복 회전과 결과 조작까지 통합 구현을 시작했다.
  - 산출물: 구현 예정
  - 검증: T4·T6·T8·T9 의존성 완료 확인
  - 리스크_또는_차단: 타이머·visibility 복구·애니메이션 완료가 같은 outcome을 중복 반영하지 않도록 세션 멱등성을 유지해야 한다.
  - 다음: 수동 1회 회전 후 반복·비우기·복사·초기화·효과음 수명주기 연결
  - 사용자_피드백: 반영 완료(T11까지 연속 진행)

- 2026-08-14 16:15 | 단계: T10 | 상태: in_progress -> done
  - 요약: 돌림판 설정부터 반복 회전·결과 조작·효과음까지 세션과 UI를 통합했다.
  - 산출물: `WheelApp`, 설정·제어·결과 컴포넌트, 기능 전용 스타일·효과음 서비스와 통합 테스트
  - 검증: 웹 테스트 22파일·117개, `npm run build:web`, `npm run verify:boundaries`, `git diff --check` 통과
  - 리스크_또는_차단: 자연스러운 회전 감각과 320px 레이아웃은 실제 브라우저에서 확인·조정해야 한다.
  - 다음: T11 반응형·접근성·시각 검수
  - 사용자_피드백: 반영 완료(T11까지 연속 진행)

- 2026-08-14 16:15 | 단계: T11 | 상태: not_started -> in_progress
  - 요약: 실제 브라우저에서 선택·돌림판 핵심 흐름과 반응형·키보드·동작 감소 상태 검수를 시작했다.
  - 산출물: 검수 및 미세 조정 예정
  - 검증: 자동 접근성 이름, live region, 320px 미디어 쿼리 구현 상태 확인
  - 리스크_또는_차단: 실제 브라우저의 SVG 레이블·포인터 정렬과 4초 감속 체감은 정적 테스트만으로 확정할 수 없다.
  - 다음: 로컬 앱 데스크톱·모바일 스모크와 회전 파라미터 미세 조정
  - 사용자_피드백: 반영 완료(T11까지 연속 진행)

- 2026-08-14 16:25 | 단계: T11 | 상태: in_progress -> done
  - 요약: 선택 셸과 돌림판의 반응형·접근성·회전 시각 검수를 완료하고 발견한 고밀도 레이블과 로또 상단 겹침을 수정했다.
  - 산출물: 45개 구획 번호 외곽 배치, 셸 상단 내비게이션, 공통 전역 스타일의 셸 이동, 키보드 선택 회귀 테스트
  - 검증: Chrome·인앱 Chromium 데스크톱 및 320px 스모크, Chrome 4초 회전 결과 공개, 목표 구획 중심 정지, 8개·45개 후보, 가로 넘침 없음, 앱 콘솔 오류 없음. 웹 테스트 22파일·118개, `npm run build:web`, `npm run verify:boundaries`, `git diff --check` 통과
  - 리스크_또는_차단: 현재 브라우저 제어 환경에는 Safari·Firefox·Edge가 없어 해당 실기 검수는 T12 출시 검증의 잔여 항목이다. 동작 감소 결과·220ms 전환은 자동 테스트로 확인했다.
  - 다음: 사용자 피드백 후 T12 전체 통합·회귀·빌드 검증
  - 사용자_피드백: 대기(T11 완료 피드백 게이트)

- 2026-08-14 15:42 | 단계: T2 | 상태: not_started -> in_progress
  - 요약: 웹 기능 하위 경계 검증 구현을 시작했다.
  - 산출물: T2 진행 상태와 연속 진행 승인을 `TASK.md`에 반영했다.
  - 검증: 기존 `npm run verify:boundaries` 기준선 통과
  - 리스크_또는_차단: 아직 기능 디렉터리가 없으므로 경계 판정 fixture가 이동 전·후 경로를 모두 검증해야 한다.
  - 다음: 순수 경계 판정과 허용·금지 fixture를 구현한다.
  - 사용자_피드백: 반영 완료(T2부터 T5까지 중간 피드백 없이 연속 진행 승인)

- 2026-08-14 15:43 | 단계: T2 | 상태: in_progress -> done
  - 요약: 웹 하위 기능의 허용 import 방향과 공개 진입점 규칙을 자동화했다.
  - 산출물: `web-feature-boundaries.mjs`, 6개 fixture 테스트, 갱신된 실제 경계 검증기와 package script
  - 검증: `npm run test:boundaries`, `npm run verify:boundaries`, `git diff --check` 통과
  - 리스크_또는_차단: 실제 기능 이동 후 새 경로를 대상으로 재검증해야 한다.
  - 다음: T3 기존 로또 기능 수직 격리
  - 사용자_피드백: 반영 완료(T5까지 연속 진행)

- 2026-08-14 15:43 | 단계: T3 | 상태: not_started -> in_progress
  - 요약: 기존 로또 기능의 동작 보존 이동을 시작했다.
  - 산출물: 이동 대상 파일 목록 확정
  - 검증: T2 경계 테스트 통과 상태 확인
  - 리스크_또는_차단: 대규모 경로 이동이므로 import와 CSS 진입점만 변경하고 내부 동작은 수정하지 않아야 한다.
  - 다음: 로또 전용 파일 이동과 공개 진입점 구성
  - 사용자_피드백: 반영 완료(T5까지 연속 진행)

- 2026-08-14 15:46 | 단계: T3 | 상태: in_progress -> done
  - 요약: 기존 로또 웹 기능을 동작 변경 없이 `features/lottery`로 수직 격리했다.
  - 산출물: `LotteryApp` 공개 진입점, 이동된 로또 도메인·UI·서비스·테스트·스타일, 얇은 루트 App
  - 검증: 웹 테스트 13파일·71개, `npm run build:web`, `npm run verify:boundaries`, `git diff --check` 통과
  - 리스크_또는_차단: T4 셸 추가 시 로또 내부가 아니라 상위 래퍼만 변경해야 한다.
  - 다음: T4 추첨기 선택 웹 셸
  - 사용자_피드백: 반영 완료(T5까지 연속 진행)

- 2026-08-14 15:46 | 단계: T4 | 상태: not_started -> in_progress
  - 요약: 선택 화면과 기능 내비게이션 구현을 시작했다.
  - 산출물: 구현 예정
  - 검증: T3 회귀 검증 통과 상태 확인
  - 리스크_또는_차단: 직접 주소 최초 진입에서도 뒤로 가기가 선택 화면으로 돌아오도록 history 상태를 구성해야 한다.
  - 다음: 셸 타입·저장소·내비게이션·오류 경계와 테스트 구현
  - 사용자_피드백: 반영 완료(T5까지 연속 진행)

- 2026-08-14 15:53 | 단계: T4 | 상태: in_progress -> done
  - 요약: 기능 내부 상태를 모르는 hash 선택 셸과 셸 전용 마지막 선택 저장소를 구현했다.
  - 산출물: 선택 메타데이터·저장소, 기능 오류 경계, 로또·돌림판 공개 진입점 마운트, 선택 셸 UI·테스트
  - 검증: 웹 테스트 15파일·82개, `npm run build:web`, `npm run verify:boundaries`, `git diff --check` 통과
  - 리스크_또는_차단: 돌림판 공개 진입점은 T6~T10 구현 전까지 의도적인 placeholder다.
  - 다음: T5 공통 후보·입력·난수 경계 점검
  - 사용자_피드백: 반영 완료(T5까지 연속 진행)

- 2026-08-14 15:53 | 단계: T5 | 상태: not_started -> in_progress
  - 요약: 돌림판 후보 정책과 현재 공통 코어의 책임을 대조하기 시작했다.
  - 산출물: 점검 진행 중
  - 검증: `parseNames`의 2~45개·20자 제한과 `secureRandomIndex` 공개 상태 확인
  - 리스크_또는_차단: 비복원 함수는 기존 로또·MCP 계약이므로 돌림판 세션에서 참조하지 않아야 한다.
  - 다음: core·MCP 회귀 테스트와 빌드로 변경 불필요 판단을 확정한다.
  - 사용자_피드백: 반영 완료(T5까지 연속 진행)

- 2026-08-14 15:54 | 단계: T5 | 상태: in_progress -> done
  - 요약: 기존 코어가 돌림판에 필요한 입력 문법과 단일 난수 프리미티브를 이미 제공해 추가 추상화나 코드 변경이 불필요하다고 확정했다.
  - 산출물: 코어 무변경 결정과 T6 의존 계약을 `TASK.md`에 기록
  - 검증: core 테스트 3파일·20개, MCP 테스트 3파일·17개, `npm run build:mcp` 통과
  - 리스크_또는_차단: T6는 복원 추첨을 위해 `secureRandomIndex`만 사용하고 `drawCandidates`·`createDrawOrder`를 사용하지 않아야 한다.
  - 다음: 사용자 피드백 후 T6 돌림판 복원 추첨 세션 엔진
  - 사용자_피드백: 대기(T5 완료 피드백 게이트)

- 2026-08-14 15:16 | 단계: T1 | 상태: not_started -> in_progress
  - 요약: 돌림판 1차 제품 요구사항 인터뷰를 시작했다.
  - 산출물: 요구사항 인터뷰 진행 상태를 `TASK.md`에 반영했다.
  - 검증: T1의 선행 의존성이 없고 미확정 질문이 Summary에 기록되어 있음을 확인했다.
  - 리스크_또는_차단: 복원 정책, 진행 방식과 종료 조건이 확정되기 전에는 세션·UI 구현 계약을 결정할 수 없다.
  - 다음: 핵심 추첨 흐름을 먼저 합의한 뒤 표현·저장·접근성 범위를 확인한다.
  - 사용자_피드백: 반영 완료(복원 추첨 고정, 수동 전용, 무제한 회전 이력 누적 승인)

- 2026-08-14 15:17 | 단계: T1 | 상태: in_progress
  - 요약: 핵심 추첨 흐름에 대한 사용자 피드백을 반영했다.
  - 산출물: T1 progress와 Summary의 미결정 항목을 갱신했다.
  - 검증: 확정된 흐름이 `DESIGN.md`의 독립 회전·고유 outcome ID 불변식과 충돌하지 않음을 확인했다.
  - 리스크_또는_차단: 입력 복원, 결과 초기화와 부가 기능 범위가 남아 있다.
  - 다음: 입력·저장·결과 이력 수명주기를 합의한다.
  - 사용자_피드백: 반영 완료(기존 입력 문법 재사용, 후보·설정만 저장, 결과 이력 비영속화 승인)

- 2026-08-14 15:18 | 단계: T1 | 상태: in_progress
  - 요약: 후보 입력과 저장·초기화 수명주기를 확정했다.
  - 산출물: T1 progress와 미결정 항목을 갱신했다.
  - 검증: 로또 저장 키를 유지하면서 돌림판 전용 저장 키를 사용하는 DESIGN의 격리 원칙과 일치함을 확인했다.
  - 리스크_또는_차단: 회전 연출, 고밀도 레이블, 부가 기능과 내비게이션 계약이 남아 있다.
  - 다음: 회전 시간과 후보 표시 경험을 합의한다.
  - 사용자_피드백: 반영 완료(약 4초 자연스러운 회전, 동작 감소 대응, 전체 후보·결과 이름 제공 승인)

- 2026-08-14 15:20 | 단계: T1 | 상태: in_progress
  - 요약: 회전 시간, 동작 감소 대응과 고밀도 후보 표시 기준을 확정했다.
  - 산출물: T1 progress와 미결정 항목을 갱신했다.
  - 검증: 애니메이션이 이미 확정된 결과를 표현하기만 한다는 DESIGN 불변식을 유지했다.
  - 리스크_또는_차단: 자연스러운 감속의 세부 값은 구현 후 실제 브라우저 테스트에서 조정해야 한다.
  - 다음: 결과 부가 기능과 추첨기 선택 내비게이션을 합의한다.
  - 사용자_피드백: 반영 완료(구현 후 테스트 단계에서 회전 연출 미세 조정 요청)

- 2026-08-14 15:23 | 단계: T1 | 상태: in_progress
  - 요약: 부가 기능과 추첨기 선택 내비게이션 범위를 확정했다.
  - 산출물: T1 progress와 Summary의 미결정 항목을 갱신했다.
  - 검증: 기능별 저장소 격리, 결과 비영속화와 웹 셸 책임이 DESIGN의 결정과 일치함을 확인했다.
  - 리스크_또는_차단: PRD·SPEC 작성 전 추가 누락 요구사항 여부의 최종 확인이 필요하다.
  - 다음: 추가 요구가 없음을 확인한 뒤 feature `PRD.md`와 `SPEC.md`를 작성한다.
  - 사용자_피드백: 반영 완료(돌림판 추가가 기존 로또 추첨기 동작에 영향을 주지 않아야 함)

- 2026-08-14 15:24 | 단계: T1 | 상태: in_progress
  - 요약: 기존 로또 기능 무변경을 최상위 비회귀 요구사항으로 확정했다.
  - 산출물: Summary의 적용 범위와 T1 acceptance·progress를 갱신했다.
  - 검증: T3의 동작 보존 이동과 T12의 기존 SPEC 전체 회귀 기준이 해당 요구사항을 추적하고 있음을 확인했다.
  - 리스크_또는_차단: 선택 화면 추가는 의도된 셸 변경이며 로또 기능 내부 변경과 구분해 검증해야 한다.
  - 다음: 추가 요구가 없음을 확인한 뒤 feature `PRD.md`와 `SPEC.md`를 작성한다.
  - 사용자_피드백: 반영 완료(인터뷰 종료)

- 2026-08-14 15:30 | 단계: T1 | 상태: in_progress -> done
  - 요약: 합의된 돌림판 제품 요구사항과 기술 계약을 문서화하고 설계의 미결정 항목을 확정했다.
  - 산출물: `PRD.md`, `SPEC.md`, 갱신된 `DESIGN.md`, `TASK.md`
  - 검증: PRD·SPEC의 범위, 상태 흐름, 저장 키, 회전 계약과 로또 비회귀 조건을 상호 대조했다.
  - 리스크_또는_차단: 자연스러운 감속의 세부 상수는 구현 후 실제 브라우저 테스트에서 조정한다.
  - 다음: 사용자 피드백 승인 후 T2 기능 경계 자동 검증을 시작한다.
  - 사용자_피드백: 승인 대기
