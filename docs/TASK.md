# TASKS

## Summary

- source: `docs/SPEC.md`
- supplemental_source: 없음 (`PRD.md` 미사용)
- last_updated: 2026-07-28
- overall_status: `done`
- progress: 28/28 tasks done
- open_questions: 없음
- next_task: 없음
- workflow_feedback: 단계별 승인 없이 전체 작업 연속 진행 승인
- scope_note: 배포 설정·자동화와 SPEC의 `초기 범위 밖` 항목은 이 계획에 포함하지 않는다.

## Execution Order

1. 기반 구성: T1
2. 독립 핵심 모듈: T2, T3, T5
3. 상태 엔진과 설정 UI: T4, T6
4. 시각화와 추첨 흐름: T7, T8, T9
5. 부가 경험과 스타일: T10, T11
6. 통합 검증과 출시 확인: T12, T13
7. 후속 작업 문서화: T14
8. 공 개수 확장: T15
9. 추첨 목표 개수 설정: T16
10. 숫자 범위 입력: T17
11. 3D 구형 투영 혼합 연출: T18
12. 자동 추첨 간격 단축: T19
13. 동일 크기·중앙 횡단 혼합 개선: T20
14. 완료 후 잔여 공 중력 정착: T21
15. 수동 모드 상시 혼합: T22
16. 추첨 화면 상단 안내 문구 변경: T23
17. 설정 화면 소개 문구 개선: T24
18. 설정 화면 히어로 제목 변경: T25
19. 중복 입력 설명 제거: T26
20. 이름·숫자 입력 안내 문구 추가: T27
21. 중복 공 반복 표현식: T28

## Tasks

- [x] T1 | done | React·TypeScript·Vite 프로젝트 기반 구성
  - scope: 정적 SPA를 실행·테스트·빌드할 수 있는 최소 프로젝트 구조, npm 스크립트, TypeScript 설정, 테스트 러너 구성
  - deliverables: `package.json`, Vite 엔트리, 앱 루트, TypeScript 설정, 테스트 설정, 기본 전역 스타일
  - acceptance:
    - 개발 서버가 빈 앱을 렌더링한다.
    - `typecheck`, `test`, `build` 명령이 실행된다.
    - 프로덕션 빌드가 서버 API 없이 정적 파일로 생성된다.
  - dependencies: 없음
  - next_action: 완료
  - progress: React 19, TypeScript, Vite, Vitest 기반 정적 SPA 구성과 기본 렌더링 테스트 완료
  - notes: `npm run typecheck`, `npm test`, `npm run build` 통과

- [x] T2 | done | 이름 파싱·검증과 핵심 데이터 모델 구현
  - scope: `Ball`, `DrawMode`, `DrawPhase`, `DrawResult`, `ScheduledDraw`, `DrawSession` 타입과 `parseNames` 구현
  - acceptance:
    - 줄바꿈과 콤마를 함께 구분자로 처리한다.
    - 앞뒤 공백과 빈 항목을 제거한다.
    - 2~45개와 이름당 20자 제한을 검증한다.
    - 중복 이름을 허용하고 세션 내 서로 다른 공 ID를 부여한다.
    - 제한 초과 입력을 자르거나 일부만 선택하지 않는다.
    - 파싱·검증 경계 조건 단위 테스트가 통과한다.
  - dependencies: T1
  - next_action: 완료
  - progress: 도메인 타입, 이름 파싱·검증, 중복 이름용 고유 공 ID 구현 완료
  - notes: 파싱·검증·중복 ID 단위 테스트 통과

- [x] T3 | done | 암호학적 난수와 자동 추첨 일정 구현
  - scope: `secureRandomIndex`, `createDrawOrder`, `createAutoSchedule`, `reconcileScheduledDraws` 구현
  - acceptance:
    - `crypto.getRandomValues()`와 거부 샘플링으로 편향 없는 인덱스를 생성한다.
    - 비보안 난수 대체 경로가 없다.
    - 모든 공 ID가 정확히 한 번 포함된 무작위 순열을 만든다.
    - 첫 공부터 3~7초 사이 정수 간격을 균등하게 누적한다.
    - 경과 일정 복구가 결과 순서를 보존하고 여러 번 호출해도 중복 반영되지 않는다.
    - Web Crypto 실패가 호출자에게 명시적 오류로 전달된다.
  - dependencies: T1, T2
  - next_action: 완료
  - progress: Web Crypto 거부 샘플링, 무작위 순열, 현재 3~7초 누적 일정 구현 완료
  - notes: 결정적 난수 주입 테스트와 실패 분기 통과

- [x] T4 | done | 추첨 세션 상태 엔진 구현
  - scope: 설정·수동 대기·혼합·자동 진행·완료·오류 상태 전이와 비복원 추첨 세션 관리
  - acceptance:
    - 수동 모드에서 한 번에 하나의 공만 선택하고 선택된 공을 남은 목록에서 제거한다.
    - 자동 모드에서 T3의 순서와 일정을 세션에 적용한다.
    - 마지막 공 이후 완료 상태가 된다.
    - 난수 실패 시 추첨을 중단하고 오류 상태가 된다.
    - `처음부터 다시`가 진행 중 작업을 취소하고 입력을 보존한 설정 상태로 돌아간다.
    - 새로고침으로 복원할 추첨 상태를 생성하지 않는다.
    - 중복 이벤트와 연속 클릭에도 결과가 한 번만 반영된다.
  - dependencies: T2, T3
  - next_action: 완료
  - progress: 수동·자동 세션, 비복원 결과 반영, 오류·완료·초기화 전이 구현 완료
  - notes: 중복 이벤트와 백그라운드 일정 복구 멱등성 테스트 통과

- [x] T5 | done | 이름 목록 브라우저 저장 구현
  - scope: `NameStorage`의 저장·복원·삭제·버전 처리와 실패 결과 모델
  - acceptance:
    - `{ version: 1, rawInput: string }` 형식으로 입력 원문만 `localStorage`에 저장한다.
    - 초기 로드 시 유효한 저장 데이터를 복원한다.
    - `모두 지우기`가 입력값과 저장값을 함께 삭제한다.
    - 모드, 남은 공, 추첨 결과와 일정은 저장하지 않는다.
    - 접근 거부·용량 초과·손상 데이터에서 앱이 중단되지 않고 경고 가능 상태를 반환한다.
  - dependencies: T1
  - next_action: 완료
  - progress: 버전 저장 계약과 저장·복원·삭제·손상·접근 실패 처리 구현 완료
  - notes: 메모리 저장소 기반 성공·실패 테스트 통과

- [x] T6 | done | 설정 화면과 앱 상태 연결
  - scope: `SetupPanel`, 한국어 입력·검증 UI, 모드 선택, 효과음 초기 토글, 시작, 모두 지우기
  - acceptance:
    - 단일 텍스트 영역에 이름을 일괄 입력할 수 있다.
    - 검증 원인을 한국어로 표시하고 오류 중에는 시작 버튼을 비활성화한다.
    - 수동·자동 모드를 시작 전에 선택할 수 있다.
    - 추첨 시작 후 입력과 모드 선택이 비활성화된다.
    - 저장된 입력이 최초 화면에 복원된다.
    - 저장 실패 시 현재 입력을 유지하면서 경고를 표시한다.
  - dependencies: T2, T4, T5
  - next_action: 완료
  - progress: 입력·검증·모드·시작·삭제·저장 경고를 App 상태와 연결
  - notes: 설정 입력·저장·재마운트 복원 테스트 및 실제 브라우저 검증 통과

- [x] T7 | done | Canvas 2D 로또 추첨기 구현
  - scope: `LotteryMachine`의 원형 용기, 공 생성·이동·반사·간단한 충돌, 혼합·배출, 크기 조정
  - acceptance:
    - 최대 45개 공을 서로 구분되는 색상으로 표시한다.
    - 긴 이름은 공 안에서 말줄임표로 표시한다.
    - 수동 혼합과 자동 연속 혼합을 외부 명령으로 제어할 수 있다.
    - 선택된 공의 배출 연출을 수행하되 결과 선택에는 관여하지 않는다.
    - 리사이즈 시 Canvas를 재배치하고 추첨 세션은 유지한다.
    - 애니메이션 취소·오류가 상태 엔진의 정확성을 훼손하지 않는다.
  - dependencies: T2, T4
  - next_action: 완료
  - progress: 원형 용기, 반사·간단 충돌, 연속 혼합, 배출 오버레이, 리사이즈 구현
  - notes: 추첨 엔진과 연출 상태를 분리하고 데스크톱·모바일 브라우저에서 시각 검증

- [x] T8 | done | 수동 추첨·결과·재시작 흐름 통합
  - scope: `DrawControls`, `ResultList`, 수동 2~3초 혼합, 순차 결과, 완료, 복사, 재시작
  - acceptance:
    - `다음 공 뽑기` 후 약 2~3초 동안 혼합하고 공 하나를 배출한다.
    - 혼합·배출 중 버튼을 비활성화해 연속 클릭을 차단한다.
    - 결과를 `1번째`, `2번째` 순으로 누적하고 전체 이름을 표시한다.
    - 마지막 공 이후 완료 메시지와 결과 복사 동작을 제공한다.
    - 복사 형식이 `1. 이름`의 줄 단위 텍스트이며 성공·실패 알림을 구분한다.
    - 복사 실패 시 수동 복사 UI를 열지 않는다.
    - `처음부터 다시`가 확인창이나 즉시 재추첨 없이 설정 화면으로 돌아간다.
  - dependencies: T4, T6, T7
  - next_action: 완료
  - progress: 2.4초 수동 혼합, 중복 클릭 방지, 누적 결과, 복사, 완료·재시작 연결
  - notes: 가짜 타이머 통합 테스트와 실제 브라우저 수동 추첨·클립보드 검증 통과

- [x] T9 | done | 자동 추첨과 백그라운드 경과 복구 통합
  - scope: 카운트다운 없는 자동 진행, 예정 시각 배출, 탭 비활성·절전 복귀, 놓친 연출 처리
  - acceptance:
    - 시작 후 첫 공부터 매번 3~7초 사이에 자동 배출한다.
    - 카운트다운·예정 시각·일시정지 UI를 제공하지 않는다.
    - 화면이 보이는 동안 공이 계속 섞인다.
    - 백그라운드에서도 절대 예정 시각 기준의 논리 시간이 흐른다.
    - 복귀 시 지난 공을 순서대로 한 번만 결과에 반영하고 놓친 애니메이션을 재생하지 않는다.
    - 마지막 예정 공 이후 완료 상태가 된다.
    - 재시작 시 타이머·애니메이션·가시성 구독을 정리한다.
  - dependencies: T3, T4, T7, T8
  - next_action: 완료
  - progress: 카운트다운 없는 자동 일정, 가시성·포커스 복구, 누락 연출 생략 구현
  - notes: 가짜 시계·멱등성 테스트와 실제 브라우저 전량 자동 추첨 통과

- [x] T10 | done | 효과음과 사용자 오류 피드백 구현
  - scope: `SoundController`, 기본 음소거, 사용자 토글, 공 섞임·배출·공개 효과와 한국어 오류 알림
  - acceptance:
    - 최초 실행과 새로고침 후 효과음이 꺼져 있다.
    - 사용자가 토글로 효과음을 켜고 끌 수 있다.
    - 소리 재생 실패가 추첨 진행을 중단시키지 않는다.
    - 난수 실패는 추첨 중단 오류로 표시한다.
    - 저장·복사 실패는 합의된 경고 또는 오류 알림으로 표시한다.
    - 기술 예외 문자열을 그대로 사용자에게 노출하지 않는다.
  - dependencies: T5, T8, T9
  - next_action: 완료
  - progress: Web Audio 효과음, 기본 음소거 토글, 오류·경고·성공 메시지 구현
  - notes: 음소거·완료음·오디오 실패 격리 테스트 통과

- [x] T11 | done | 반응형 시각 디자인과 상호작용 마감
  - scope: 밝은 방송형 디자인, 투명 원형 추첨기, 공 색상, 강조 버튼, 모바일·데스크톱 레이아웃
  - acceptance:
    - 설정·수동·자동·완료 상태의 시각 계층이 일관된다.
    - 모바일과 데스크톱에서 기능 차이가 없다.
    - 일반적인 모바일 폭에서 입력·Canvas·결과가 잘리거나 겹치지 않는다.
    - 공 내부 말줄임과 결과 목록 전체 이름이 구분된다.
    - 저성능 환경에서 애니메이션이 느려져도 제어와 결과 UI는 정상 동작한다.
    - 사용자 입력을 HTML이 아닌 텍스트로만 렌더링한다.
  - dependencies: T6, T7, T8, T9, T10
  - next_action: 완료
  - progress: 밝은 방송형 디자인과 설정·추첨·결과 반응형 레이아웃 적용
  - notes: 390×844 모바일과 기본 데스크톱 화면 시각 검수, 콘솔 오류 없음

- [x] T12 | done | 자동 테스트와 통합 회귀 검증 완성
  - scope: 각 기능 TASK의 단위 테스트를 보강하고 핵심 사용자 흐름의 통합 테스트를 완성
  - acceptance:
    - SPEC 10.1의 자동 테스트 항목을 모두 테스트 케이스로 추적할 수 있다.
    - 파싱·검증·중복 ID·비복원 전량 추첨을 검증한다.
    - 난수 실패와 거부 샘플링 경계를 검증한다.
    - 수동 중복 클릭과 재시작 취소를 검증한다.
    - 자동 일정 범위·누적·복구 멱등성을 검증한다.
    - 저장·복사 실패와 결과 문자열을 검증한다.
    - 전체 테스트와 TypeScript 검사가 통과한다.
  - dependencies: T2, T3, T4, T5, T6, T8, T9, T10
  - next_action: 완료
  - progress: SPEC 10.1의 파싱·난수·엔진·저장·UI·효과음 회귀 테스트 완성
  - notes: T15 경계 테스트 보강 후 6개 테스트 파일, 29개 테스트와 TypeScript 검사 통과

- [x] T13 | done | 빌드·브라우저·스모크 출시 검수
  - scope: 정적 빌드, 최신 브라우저 핵심 흐름, 반응형 화면, 백그라운드, 저성능 조건, 별도 데스크톱 스모크 테스트
  - acceptance:
    - 프로덕션 정적 빌드가 성공한다.
    - 최신 안정 버전 Chrome, Safari, Firefox, Edge에서 핵심 기능을 확인한다.
    - 모바일·데스크톱에서 설정·추첨·완료 화면의 겹침과 잘림이 없다.
    - 수동 2~3초 및 자동 3~7초 흐름이 요구사항대로 동작한다.
    - 백그라운드 복귀 시 결과 순서와 중복 방지가 유지된다.
    - 저성능 조건에서 애니메이션보다 추첨 정확성이 우선된다.
    - 별도 데스크톱 레이아웃 스모크 테스트가 통과한다.
    - 추첨 누락·중복 또는 이름 외부 전송과 관련된 알려진 결함이 없다.
  - dependencies: T11, T12
  - next_action: 완료
  - progress: 타입·27개 테스트·정적 빌드·보안 감사와 데스크톱·모바일 Chromium 스모크 완료
  - notes: Safari·Firefox·Edge는 현재 실행 환경에 없어 웹 표준 호환성 검토만 수행했으며 실제 실행 검증은 후속 환경에서 필요

- [x] T14 | done | 아키텍처와 개발 가이드 문서화
  - scope: 완성된 코드와 검증 결과를 기준으로 사람이 이해하기 쉽고 에이전트가 후속 작업에 활용할 수 있는 문서 작성
  - deliverables: `docs/architecture.md`, `docs/development.md`
  - acceptance:
    - 두 문서에 revision과 타임존 포함 갱신 시각을 YAML frontmatter로 기록한다.
    - 아키텍처 문서에 전체 구조, 디렉터리, 주요 모듈, 데이터 흐름, 의존성, 확장 고려사항을 포함한다.
    - 개발 가이드에 빌드·구성, 프로젝트 패턴, 테스트 실행·추가 예제, 디버깅, 보안 점검을 포함한다.
    - 문서의 명령과 테스트 예제가 실제 프로젝트에서 동작하는지 검증한다.
    - 현재 구현과 다른 추측성 설명을 남기지 않는다.
  - dependencies: T13
  - next_action: 완료
  - progress: 실제 코드와 검증 결과 기반의 아키텍처·개발 가이드 작성 완료
  - notes: 문서 frontmatter, 필수 섹션, 실행 확인된 테스트 예제, 보안 점검 내용 검수 완료

- [x] T15 | done | 최대 공 개수를 45개로 확장
  - scope: 입력·검증·화면 카운터·Canvas 밀도·테스트·사용자 및 설계 문서의 최대 공 개수를 10개에서 45개로 변경
  - acceptance:
    - 이름 45개는 유효하고 46개부터 시작을 차단한다.
    - 설정 화면의 개수 배지가 45개 기준으로 표시된다.
    - 데스크톱과 모바일 Canvas에서 45개 공이 추첨 정확성을 해치지 않고 표시된다.
    - 기존 수동·자동 추첨과 저장·복사 동작이 회귀하지 않는다.
    - 자동 검사와 프로덕션 빌드가 통과한다.
    - SPEC, README, 아키텍처, 개발 가이드와 TASK가 45개 기준으로 일치한다.
  - dependencies: T14
  - next_action: 완료
  - progress: 45개 제한·UI 카운터·Canvas 밀도 및 리사이즈·테스트·문서 갱신 완료
  - notes: 타입 검사, 29개 테스트, 프로덕션 빌드, 데스크톱·390×844 모바일 Chromium 45개 스모크 통과

- [x] T16 | done | 추첨할 공 개수 선택·입력 기능 추가
  - scope: 전체 추첨과 일부 추첨을 선택하고, 일부 추첨 시 유효한 목표 개수를 입력해 해당 개수에서 세션을 완료하는 흐름 구현
  - acceptance:
    - 기본값은 입력한 공 전체 추첨이다.
    - 일부 추첨은 1부터 입력한 공 개수까지의 정수만 허용한다.
    - 수동·자동 모드 모두 목표 개수만 비복원 추첨한 뒤 완료한다.
    - 자동 모드는 목표 개수만큼의 순서와 3~7초 무작위 일정을 만든다.
    - 일부 추첨 완료 시 뽑히지 않은 공은 결과에 포함되지 않는다.
    - 설정 화면과 결과 카운터가 목표 개수를 명확히 표시한다.
    - 새로고침 시 입력 목록만 복원하고 추첨 개수 설정은 기본값으로 초기화한다.
    - 경계·수동·자동·저장 회귀 테스트와 데스크톱·모바일 스모크가 통과한다.
    - SPEC, README, 아키텍처, 개발 가이드와 TASK가 새 동작과 일치한다.
  - dependencies: T15
  - next_action: 완료
  - progress: 추첨 개수 검증·세션 완료 조건·수동/자동 일정·설정/결과 UI·테스트·문서 갱신 완료
  - notes: 타입 검사, 38개 테스트, 프로덕션 빌드 통과; 데스크톱과 390×844 모바일에서 45개 중 6개 수동 추첨 완료 및 남은 39개 표시 확인

- [x] T17 | done | 숫자 범위 입력식 지원
  - scope: 이름 입력 전체가 `시작숫자~끝숫자` 형식이면 오름차순 숫자 이름 목록으로 확장하고 기존 추첨 흐름에 전달
  - acceptance:
    - `1~45`를 입력하면 `"1"`부터 `"45"`까지 45개의 공으로 해석한다.
    - `~` 범위식은 숫자 두 개만 허용하며 일반 이름·콤마·줄바꿈과 혼합할 수 없다.
    - 시작 숫자가 끝 숫자보다 크거나 생성 개수가 2~45개를 벗어나면 오류를 표시하고 시작을 차단한다.
    - 숫자 범위도 입력 원문 그대로 브라우저에 저장하고 새로고침 후 동일하게 복원한다.
    - 기존 줄바꿈·콤마·중복 이름과 전체·일부 추첨 동작이 회귀하지 않는다.
    - SPEC, README, 아키텍처, 개발 가이드와 TASK가 범위식 동작과 일치한다.
  - dependencies: T16
  - next_action: 완료
  - progress: 범위 파싱·검증, 설정 안내, 저장·추첨 통합 테스트와 사용자·설계 문서 갱신 완료
  - notes: 타입 검사, 44개 테스트, 프로덕션 빌드 통과; 모바일 Chromium에서 `1~45` 원문 복원·45개 숫자 공 표시·혼합식 차단·콘솔 오류 없음 확인

- [x] T18 | done | 3D 구형 공간을 2D로 투영한 혼합 연출
  - scope: 평면 공 충돌 중심 움직임을 가상 깊이축·구형 경계·원근 투영·회전 난류 기반 Canvas 2D 연출로 교체
  - acceptance:
    - 공 노드가 `x`, `y`, `z` 위치와 3축 속도를 가지며 구형 공간 안에서 움직인다.
    - 화면에서는 깊이순 정렬, 원근 크기와 투명도 차이로 앞뒤 공간감을 표현한다.
    - 서로 다른 깊이의 공이 2D 화면에서 자연스럽게 겹칠 수 있다.
    - 혼합 중 회전 흐름과 시간 기반 난류로 방향·깊이가 지속해서 변한다.
    - 평면 공 간 충돌 해소를 제거해 45개에서도 막히지 않고 역동적으로 움직인다.
    - 정지 상태는 과도한 움직임을 줄이고 수동·자동 혼합 상태는 기존 시간·결과 정확성을 유지한다.
    - 모바일·데스크톱에서 원형 경계를 벗어나지 않고 이름 가독성과 성능을 유지한다.
    - 투영·구형 경계 단위 테스트, 전체 회귀 테스트, 빌드와 브라우저 시각 검수가 통과한다.
    - SPEC, README, 아키텍처, 개발 가이드와 TASK가 새 연출과 일치한다.
  - dependencies: T17
  - next_action: 완료
  - progress: 3축 운동·구형 경계·회전 난류·원근 투영·깊이 정렬·잔상과 배출 수명주기 수정, 테스트·문서 갱신 완료
  - notes: 타입 검사, 50개 테스트, 프로덕션 빌드 통과; 데스크톱 45개 혼합 프레임 변화와 390×844 모바일 겹침·경계·가독성, 연속 수동 추첨 배출 재생 방지, 콘솔 오류 없음 확인

- [x] T19 | done | 자동 추첨 간격을 3~7초로 단축
  - scope: 자동 모드의 첫 공과 이후 공 사이 암호학적 무작위 정수 간격을 5~10초에서 3~7초로 변경
  - acceptance:
    - 모든 자동 추첨 간격은 3~7초 사이 정수다.
    - 첫 공에도 같은 3~7초 범위를 적용한다.
    - 범위 양 끝 3초와 7초의 누적 예정 시각을 결정적 난수 테스트로 검증한다.
    - 카운트다운·일시정지 없이 절대 예정 시각과 백그라운드 복구 동작을 유지한다.
    - 수동 추첨의 2.4초 혼합 시간과 추첨 결과 정확성은 변경하지 않는다.
    - README, SPEC, 아키텍처, 개발 가이드와 TASK의 시간 설명이 일치한다.
  - dependencies: T18
  - next_action: 완료
  - progress: 자동 일정 범위 상수·결정적 난수 및 UI 테스트·사용자/설계 문서 갱신 완료
  - notes: 간격 선택은 기존과 동일하게 Web Crypto와 거부 샘플링을 사용한다. 타입 검사, 50개 테스트, 프로덕션 빌드와 자동 추첨 브라우저 스모크 테스트를 통과했다.

- [x] T20 | done | 공 크기를 통일하고 중앙 횡단 혼합을 강화
  - scope: 깊이별 공 크기 차이를 제거하고 바깥쪽 회전 위주의 3축 운동에 중심 흡입·횡단 흐름을 추가
  - acceptance:
    - 모든 남은 공은 깊이와 관계없이 같은 반지름으로 렌더링한다.
    - 깊이 정렬과 겹침은 유지하되 공 이름의 글자 크기도 동일하게 유지한다.
    - 혼합 중 일부 공이 원형 추첨기의 중앙 영역을 반복해서 통과한다.
    - 바깥 회전, 중앙 유입, 반대편 배출이 섞여 단일 고리 궤도로 고착되지 않는다.
    - 공은 구형 경계 안에 유지되고 추첨 순서·시간·정확성에는 영향을 주지 않는다.
    - 운동 단위 테스트, 전체 회귀 테스트, 데스크톱·모바일 브라우저 시각 검수가 통과한다.
    - README, SPEC, 아키텍처, 개발 가이드와 TASK가 개선된 연출과 일치한다.
  - dependencies: T19
  - next_action: 완료
  - progress: 깊이와 무관한 고정 반지름, 공별 횡단 목표점·중앙 제트·회전 강도 조절, 시뮬레이션 테스트와 사용자·설계 문서 갱신 완료
  - notes: Canvas 2D와 가상 3축 좌표 구조를 유지했다. 타입 검사, 52개 테스트, 프로덕션 빌드와 데스크톱·390×844 모바일 Canvas 시각 검수를 통과했다.

- [x] T21 | done | 추첨 완료 후 남은 공을 중력으로 정착
  - scope: 일부 추첨 완료 시 추첨기에 남은 공에 아래 방향 중력, 구형 바닥 반발, 공 간 충돌과 마찰을 적용
  - acceptance:
    - 추첨이 완료되고 남은 공이 있으면 혼합·대기 운동을 중단하고 중력을 적용한다.
    - 공은 원형 추첨기 경계를 벗어나지 않고 아래쪽으로 떨어진다.
    - 공끼리 겹침을 해소하고 바닥부터 여러 층으로 쌓인다.
    - 반발과 속도는 점차 감쇠해 완료 화면에서 안정적으로 정착한다.
    - 전체 추첨처럼 남은 공이 없을 때는 기존 완료 화면을 유지한다.
    - 중력 연출은 추첨 결과·순서·시간과 자동 일정에 영향을 주지 않는다.
    - 운동 단위 테스트, 전체 회귀 테스트, 데스크톱·모바일 일부 추첨 완료 화면 검수가 통과한다.
    - README, SPEC, 아키텍처, 개발 가이드와 TASK가 중력 정착 동작과 일치한다.
  - dependencies: T20
  - next_action: 완료
  - progress: 완료 상태 prop, 아래 방향 중력·구형 바닥 저반발·공 간 위치 보정/충돌·마찰·정지 임계값과 사용자·설계 문서 갱신 완료
  - notes: 기존 혼합 중에는 공 간 충돌을 사용하지 않고, 완료 후 정착 단계에서만 충돌을 계산한다. 타입 검사, 54개 테스트, 프로덕션 빌드와 데스크톱·390×844 모바일 일부 추첨 완료 화면 검수를 통과했다.

- [x] T22 | done | 수동 추첨 대기 중에도 공을 계속 혼합
  - scope: 수동 모드의 추첨 시작 후 준비·공 선택 대기 구간에도 기존 3축 혼합 운동을 유지
  - acceptance:
    - 수동 추첨을 시작하면 첫 버튼을 누르기 전부터 공이 계속 섞인다.
    - 한 공 배출 후 다음 버튼을 기다리는 동안에도 남은 공이 계속 섞인다.
    - 버튼을 누른 뒤 2.4초의 결과 확정 시간과 중복 클릭 방지는 유지한다.
    - 목표 개수에 도달하면 상시 혼합을 종료하고, 남은 공이 있으면 기존 중력 정착으로 전환한다.
    - 자동 모드의 상시 혼합과 추첨 일정은 변경하지 않는다.
    - 추첨 순서·정확성은 Canvas 프레임과 계속 분리한다.
    - 전체 회귀 테스트와 데스크톱·모바일 수동 흐름 시각 검수가 통과한다.
    - README, SPEC, 아키텍처, 개발 가이드와 TASK가 수동 상시 혼합 동작과 일치한다.
  - dependencies: T21
  - next_action: 완료
  - progress: 수동 `ready`·`mixing` 상시 혼합 조건, 완료 시 정착 전환, 상태 테스트와 사용자·설계 문서 갱신 완료
  - notes: UI의 `준비 완료`·`섞는 중` 상태 표시는 버튼 처리 가능 여부를 나타내며 Canvas 상시 혼합과 독립적으로 유지한다. 타입 검사, 55개 테스트, 프로덕션 빌드와 데스크톱·390×844 모바일 수동 대기 프레임 검수를 통과했다.

- [x] T23 | done | 추첨 화면 상단 안내 문구 변경
  - scope: 진행 중 추첨 화면의 기본 상단 제목을 `두근두근, 추첨을 시작합니다`로 교체
  - acceptance:
    - 수동·자동 추첨의 진행 중 기본 제목에 새 문구가 표시된다.
    - 완료·오류 상태의 기존 전용 제목은 유지한다.
    - 관련 UI 테스트 기대값이 새 문구와 일치한다.
  - dependencies: T22
  - next_action: 완료
  - progress: 진행 중 기본 제목과 관련 UI 테스트 기대값 변경 완료
  - notes: 설정 화면의 히어로 문구와 완료·오류 제목은 변경하지 않았다. 타입 검사, 55개 테스트와 프로덕션 빌드를 통과했다.

- [x] T24 | done | 설정 화면 소개 문구를 사용자 친화적으로 개선
  - scope: 중복 이름과 공정한 무작위 추첨을 설명하는 기술 중심 문구를 친근한 기대감 중심 문구로 교체
  - acceptance:
    - 첫 문장은 같은 이름도 각각 별도로 참여한다는 의미를 유지한다.
    - 둘째 문장은 브라우저 난수 같은 기술 용어 없이 결과를 미리 알 수 없다는 기대감을 전달한다.
    - 설정 화면 외의 기능·문구는 변경하지 않는다.
    - 관련 UI 테스트와 전체 회귀 검사가 통과한다.
  - dependencies: T23
  - next_action: 완료
  - progress: 기술 용어 없는 히어로 소개 문구와 소개 영역 UI 테스트 갱신 완료
  - notes: 적용 문구는 `같은 이름도 하나씩 따로 참여해요.`와 `누가 뽑힐지는 마지막 순간까지 아무도 몰라요.`다. 타입 검사, 55개 테스트와 프로덕션 빌드를 통과했다.

- [x] T25 | done | 설정 화면 히어로 제목 변경
  - scope: 설정 화면의 큰 제목 `이름을 넣고, 순간을 즐겨보세요.`를 `두근두근, 추첨을 시작합니다`로 교체
  - acceptance:
    - 설정 화면 큰 제목이 새 문구를 두 줄로 표시한다.
    - 제목의 접근 가능한 전체 이름은 `두근두근, 추첨을 시작합니다`다.
    - 진행 화면에 이미 적용된 같은 문구와 완료·오류 제목은 유지한다.
    - 관련 UI 테스트와 전체 회귀 검사가 통과한다.
  - dependencies: T24
  - next_action: 완료
  - progress: 두 줄 히어로 제목, 명시적 접근 이름과 관련 영역 테스트 갱신 완료
  - notes: 본문 소개 문구는 T24에서 적용한 내용을 유지했다. 타입 검사, 55개 테스트와 프로덕션 빌드를 통과했다.

- [x] T26 | done | 설정 화면에서 중복 이름 설명 제거
  - scope: 히어로 본문에서 중복 이름 입력 가능성을 설명하는 첫 문장을 제거하고 기대감 문구만 유지
  - acceptance:
    - `같은 이름도 하나씩 따로 참여해요.` 문구가 설정 화면에서 제거된다.
    - `누가 뽑힐지는 마지막 순간까지 아무도 몰라요.` 문구는 유지한다.
    - 중복 이름 허용 기능과 입력 검증은 변경하지 않는다.
    - 관련 UI 테스트와 전체 회귀 검사가 통과한다.
  - dependencies: T25
  - next_action: 완료
  - progress: 중복 입력 설명과 줄바꿈 제거, 기대감 문구 유지 및 UI 테스트 갱신 완료
  - notes: 기능 설명보다 추첨 경험의 기대감을 우선했다. 중복 이름 허용 기능은 유지하며 타입 검사, 55개 테스트와 프로덕션 빌드를 통과했다.

- [x] T27 | done | 설정 화면에 이름·숫자 입력 안내 문구 추가
  - scope: 중복 이름 설명이 있던 자리에 이름이나 숫자를 원하는 대로 입력할 수 있다는 친근한 안내를 추가
  - acceptance:
    - 첫 문장에 `이름도 숫자도, 원하는 대로 넣어보세요.`를 표시한다.
    - 둘째 문장의 `누가 뽑힐지는 마지막 순간까지 아무도 몰라요.`는 유지한다.
    - 실제 이름 목록·숫자 범위 입력 기능과 검증은 변경하지 않는다.
    - 관련 UI 테스트와 전체 회귀 검사가 통과한다.
  - dependencies: T26
  - next_action: 완료
  - progress: 이름·숫자 입력 안내 첫 문장과 소개 영역 UI 테스트 갱신 완료
  - notes: 숫자 입력은 기존 숫자 이름과 `1~45` 범위 문법을 그대로 사용한다. 타입 검사, 55개 테스트와 프로덕션 빌드를 통과했다.

- [x] T28 | done | 중복 공 반복 표현식 지원
  - scope: 줄바꿈·콤마 입력 항목에서 `이름*반복횟수` 문법을 해석해 이름 또는 숫자 공을 지정 횟수만큼 확장
  - acceptance:
    - `민지*2, 준호*3`을 민지 두 개와 준호 세 개의 독립된 공으로 해석한다.
    - `7*2` 같은 숫자 반복식과 `*` 앞뒤 공백을 지원한다.
    - 반복 횟수는 1~45 사이 정수만 허용하고 잘못된 형식은 한국어 오류로 안내한다.
    - 확장된 전체 공 개수 2~45개와 이름당 20자 제한을 기존과 동일하게 검증한다.
    - 기존 줄바꿈·콤마·중복 입력과 단독 숫자 범위 문법이 회귀하지 않는다.
    - 입력 안내, README, SPEC과 설계·개발 문서가 새 문법을 설명한다.
    - 관련 단위·UI 테스트, 타입 검사와 프로덕션 빌드가 통과한다.
  - dependencies: T27
  - next_action: 완료
  - progress: 반복식 파싱·검증, 설정 화면 안내, 단위·UI 테스트와 관련 문서 갱신 완료
  - notes: `*`는 반복식 전용 예약 문자이며 숫자 범위식과 혼합하지 않는다. 타입 검사, 66개 테스트와 프로덕션 빌드를 통과했고 실제 브라우저에서 유효·오류·기존 범위 입력을 검증했다.

## 진행 로그

- 2026-07-28 13:50 | 단계: T1 | 상태: not_started -> in_progress
  - 요약: React·TypeScript·Vite 프로젝트 기반 구성 시작
  - 산출물: 작업 중
  - 검증: 예정 (`typecheck`, `test`, `build`)
  - 리스크_또는_차단: 없음
  - 다음: 프로젝트 파일 생성 및 의존성 설치
  - 사용자_피드백: 구현 시작 및 단계별 승인 없이 전체 작업 연속 진행 요청 반영

- 2026-07-28 13:53 | 단계: T1 | 상태: in_progress -> done
  - 요약: React·TypeScript·Vite 정적 SPA와 Vitest 테스트 기반 구성
  - 산출물: `package.json`, `package-lock.json`, Vite·TypeScript 설정, `index.html`, `src/` 앱 기반
  - 검증: typecheck 통과, 1개 테스트 통과, 프로덕션 정적 빌드 통과
  - 리스크_또는_차단: 없음
  - 다음: T2 이름 파싱·검증과 핵심 데이터 모델
  - 사용자_피드백: 단계별 승인 없이 연속 진행 승인 반영

- 2026-07-28 13:53 | 단계: T2 | 상태: not_started -> in_progress
  - 요약: 도메인 타입과 이름 파싱·검증 구현 시작
  - 산출물: 작업 중
  - 검증: 예정 (파싱·검증 단위 테스트)
  - 리스크_또는_차단: 없음
  - 다음: 테스트 우선으로 도메인 모듈 구현
  - 사용자_피드백: 연속 진행 승인 반영

- 2026-07-28 13:56 | 단계: T2 | 상태: in_progress -> done
  - 요약: 이름 파싱·검증, 공 데이터 모델과 중복 이름 식별 구현
  - 산출물: `src/domain/types.ts`, `src/domain/names.ts`
  - 검증: 관련 단위 테스트 통과
  - 리스크_또는_차단: 없음
  - 다음: T3
  - 사용자_피드백: 연속 진행 승인 반영

- 2026-07-28 13:56 | 단계: T3 | 상태: not_started -> done
  - 요약: Web Crypto 거부 샘플링, 무작위 순열과 자동 일정 구현
  - 산출물: `src/domain/random.ts`
  - 검증: 편향 구간 재추출·난수 실패·일정 범위 테스트 통과
  - 리스크_또는_차단: 없음
  - 다음: T4
  - 사용자_피드백: 연속 진행 승인 반영

- 2026-07-28 13:56 | 단계: T4 | 상태: not_started -> done
  - 요약: UI와 독립된 수동·자동 추첨 상태 엔진 구현
  - 산출물: `src/domain/drawEngine.ts`
  - 검증: 비복원·중복 방지·완료·오류·일정 복구 테스트 통과
  - 리스크_또는_차단: 없음
  - 다음: T5
  - 사용자_피드백: 연속 진행 승인 반영

- 2026-07-28 13:56 | 단계: T5 | 상태: not_started -> done
  - 요약: 이름 원문 전용 브라우저 저장 어댑터 구현
  - 산출물: `src/services/nameStorage.ts`
  - 검증: 저장·복원·삭제·손상·접근 실패 테스트 통과
  - 리스크_또는_차단: 없음
  - 다음: T6
  - 사용자_피드백: 연속 진행 승인 반영

- 2026-07-28 13:56 | 단계: T6 | 상태: not_started -> in_progress
  - 요약: 설정 화면과 앱 상태 통합 시작
  - 산출물: 작업 중
  - 검증: 예정 (사용자 입력과 검증 UI 테스트)
  - 리스크_또는_차단: 없음
  - 다음: SetupPanel과 App 상태 구현
  - 사용자_피드백: 연속 진행 승인 반영

- 2026-07-28 14:15 | 단계: T6 | 상태: in_progress -> done
  - 요약: 설정 입력·검증·모드와 브라우저 저장을 앱 상태에 통합
  - 산출물: `src/components/SetupPanel.tsx`, `src/App.tsx`
  - 검증: UI 통합 테스트와 브라우저 설정 화면 검증 통과
  - 리스크_또는_차단: 없음
  - 다음: T7
  - 사용자_피드백: 연속 진행 승인 반영

- 2026-07-28 14:15 | 단계: T7 | 상태: not_started -> done
  - 요약: 추첨 결과와 독립된 Canvas 2D 로또 기계 연출 구현
  - 산출물: `src/components/LotteryMachine.tsx`
  - 검증: 타입·빌드 통과, 데스크톱·모바일 시각 검수
  - 리스크_또는_차단: 단순 충돌 연출의 고도화는 SPEC상 후속 범위
  - 다음: T8
  - 사용자_피드백: 연속 진행 승인 반영

- 2026-07-28 14:15 | 단계: T8 | 상태: not_started -> done
  - 요약: 수동 추첨, 결과 누적·복사, 완료·재시작 통합
  - 산출물: `DrawControls`, `ResultList`, App 수동 흐름
  - 검증: 가짜 타이머 테스트와 실제 브라우저 클립보드 확인
  - 리스크_또는_차단: 없음
  - 다음: T9
  - 사용자_피드백: 연속 진행 승인 반영

- 2026-07-28 14:15 | 단계: T9 | 상태: not_started -> done
  - 요약: 무작위 자동 간격과 절대 시각 기반 백그라운드 복구 통합
  - 산출물: App 자동 스케줄러와 가시성·포커스 처리
  - 검증: 자동 일정 회귀 테스트와 실제 브라우저 전량 추첨
  - 리스크_또는_차단: 운영체제 수준 백그라운드 실행은 SPEC대로 비보장
  - 다음: T10
  - 사용자_피드백: 연속 진행 승인 반영

- 2026-07-28 14:15 | 단계: T10 | 상태: not_started -> done
  - 요약: 기본 음소거 효과음과 실패 격리형 사용자 피드백 구현
  - 산출물: `SoundController`, `SoundToggle`, notice UI
  - 검증: 효과음·오류 처리 테스트 통과
  - 리스크_또는_차단: 없음
  - 다음: T11
  - 사용자_피드백: 연속 진행 승인 반영

- 2026-07-28 14:15 | 단계: T11 | 상태: not_started -> done
  - 요약: 모바일·데스크톱 방송형 반응형 디자인 완성
  - 산출물: `src/index.css`, 상태별 레이아웃
  - 검증: 데스크톱과 390×844 모바일 시각 검수, 브라우저 콘솔 오류 없음
  - 리스크_또는_차단: 없음
  - 다음: T12
  - 사용자_피드백: 연속 진행 승인 반영

- 2026-07-28 14:16 | 단계: T12 | 상태: not_started -> done
  - 요약: SPEC 자동 테스트 항목과 UI 통합 회귀 보강
  - 산출물: 도메인·저장·효과음·App 테스트 6개 파일
  - 검증: 27개 테스트와 TypeScript 검사 통과
  - 리스크_또는_차단: 없음
  - 다음: T13
  - 사용자_피드백: 연속 진행 승인 반영

- 2026-07-28 14:16 | 단계: T13 | 상태: not_started -> in_progress
  - 요약: 최종 출시 검수 시작
  - 산출물: 검증 결과 정리 중
  - 검증: 정적 빌드·데스크톱·모바일 스모크 선행 통과
  - 리스크_또는_차단: 없음
  - 다음: 저장소·보안 점검 후 T14 문서화
  - 사용자_피드백: 연속 진행 승인 반영

- 2026-07-28 14:17 | 단계: T13 | 상태: in_progress -> done
  - 요약: 정적 빌드, 자동 검사, 브라우저 레이아웃·흐름, 보안 의존성 검수
  - 산출물: 프로덕션 `dist/`, 브라우저 스모크 및 보안 감사 결과
  - 검증: typecheck, 27 tests, build, npm audit 통과; 데스크톱·390×844 모바일 Chromium 스모크와 콘솔 오류 점검 통과
  - 리스크_또는_차단: Safari·Firefox·Edge 실제 실행은 현재 환경에서 미검증
  - 다음: T14
  - 사용자_피드백: 연속 진행 승인 반영

- 2026-07-28 14:17 | 단계: T14 | 상태: not_started -> in_progress
  - 요약: 실제 코드 기반 아키텍처·개발 가이드 작성 시작
  - 산출물: 작업 중 (`docs/architecture.md`, `docs/development.md`)
  - 검증: 문서 내 테스트 예제 선행 실행 통과
  - 리스크_또는_차단: Git 저장소가 아니므로 revision은 `unversioned`로 기록
  - 다음: 문서 작성 및 링크·명령 검수
  - 사용자_피드백: 구현 후 후속 작업용 산출물 요청 반영

- 2026-07-28 14:18 | 단계: T14 | 상태: in_progress -> done
  - 요약: 구현 구조와 개발·검증 방법을 후속 작업자가 재사용할 수 있게 문서화
  - 산출물: `docs/architecture.md`, `docs/development.md`
  - 검증: YAML frontmatter, 필수 섹션, 코드 블록 균형, 실제 테스트 예제 실행 확인
  - 리스크_또는_차단: Git 저장소가 아니므로 revision은 `unversioned`
  - 다음: 전체 작업 완료
  - 사용자_피드백: 구현 후 사람이 읽고 에이전트가 참고할 산출물 요청 반영

- 2026-07-28 14:25 | 단계: T15 | 상태: not_started -> in_progress
  - 요약: 최대 공 개수를 10개에서 45개로 확장 시작
  - 산출물: 작업 중
  - 검증: 예정 (45/46개 경계, Canvas 데스크톱·모바일, 회귀 테스트, 빌드)
  - 리스크_또는_차단: 공 밀도 증가에 따른 모바일 가독성과 애니메이션 성능 확인 필요
  - 다음: 코드·테스트·문서 갱신
  - 사용자_피드백: 최대 공 45개 요청 반영

- 2026-07-28 14:29 | 단계: T15 | 상태: in_progress -> done
  - 요약: 최대 공 45개 확장과 고밀도 Canvas 배치·리사이즈 개선 완료
  - 산출물: 입력 제한·카운터·Canvas·경계 테스트, `README.md`, `docs/` 문서 갱신
  - 검증: typecheck, 29 tests, build 통과; 데스크톱·390×844 모바일에서 45개 표시와 첫 수동 추첨 확인; 브라우저 오류 없음
  - 리스크_또는_차단: 46개 이상 확장은 별도 성능·UX 검증 필요
  - 다음: 전체 작업 완료
  - 사용자_피드백: 최대 공 45개 요청 반영

- 2026-07-28 14:30 | 단계: T16 | 상태: not_started -> in_progress
  - 요약: 전체 또는 일부 추첨 목표 개수 설정 기능 구현 시작
  - 산출물: 작업 중
  - 검증: 예정 (목표 개수 경계, 수동·자동 완료 조건, 저장 회귀, 브라우저 스모크)
  - 리스크_또는_차단: 없음
  - 다음: 도메인 세션과 설정 UI에 목표 개수 적용
  - 사용자_피드백: 45개 중 6개처럼 추첨할 개수를 선택 또는 입력하는 기능 요청 반영

- 2026-07-28 14:37 | 단계: T16 | 상태: in_progress -> done
  - 요약: 전체 또는 일부 추첨 개수 설정과 목표 기반 완료 흐름 구현 완료
  - 산출물: `drawCount` 검증·세션 모델, 설정·결과 UI, 38개 테스트, SPEC·README·아키텍처·개발 가이드 갱신
  - 검증: typecheck, 38 tests, build 통과; 데스크톱·390×844 모바일에서 45개 중 6개 수동 추첨을 끝까지 실행해 6/6 완료·남은 공 39개·콘솔 오류 없음 확인
  - 리스크_또는_차단: 없음
  - 다음: 전체 작업 완료
  - 사용자_피드백: 추첨할 개수 선택 또는 입력 기능 요청 반영

- 2026-07-28 14:48 | 단계: T17 | 상태: not_started -> in_progress
  - 요약: `1~45` 형식의 숫자 범위 입력 지원 시작
  - 산출물: 작업 중
  - 검증: 예정 (정상 범위, 잘못된 형식·역순·개수 경계, 기존 입력·저장·추첨 회귀)
  - 리스크_또는_차단: 없음
  - 다음: 파서·UI 안내·테스트·문서 갱신
  - 사용자_피드백: 숫자 범위를 순차 이름 목록으로 해석하는 기능 요청 반영

- 2026-07-28 14:50 | 단계: T17 | 상태: in_progress -> done
  - 요약: `시작숫자~끝숫자` 단독 범위식을 순차 숫자 공으로 확장하는 기능 완료
  - 산출물: 범위 파서·오류 메시지·설정 안내·44개 테스트, README와 SPEC·아키텍처·개발 가이드 갱신
  - 검증: typecheck, 44 tests, build 통과; `1~45` 새로고침 원문 복원 후 45개 공 시작, 혼합식 차단, 390×844 모바일 가로 넘침과 브라우저 콘솔 오류 없음 확인
  - 리스크_또는_차단: 없음
  - 다음: 전체 작업 완료
  - 사용자_피드백: 숫자 범위 입력 요청 반영

- 2026-07-28 14:55 | 단계: T18 | 상태: not_started -> in_progress
  - 요약: 평면 충돌을 3D 구형 공간의 2D 투영 연출로 교체 시작
  - 산출물: 작업 중
  - 검증: 예정 (3축 경계·원근·겹침 단위 테스트, 45개 수동·자동 혼합 시각 검수)
  - 리스크_또는_차단: 깊이 표현이 과하면 이름 가독성이 낮아질 수 있어 모바일·데스크톱에서 크기·투명도 범위를 함께 검수
  - 다음: 운동 모듈 구현 후 Canvas 렌더링 연결
  - 사용자_피드백: 실제 구형 추첨기를 2D로 전사한 듯한 겹침과 역동적 혼합 요청 반영

- 2026-07-28 15:01 | 단계: T18 | 상태: in_progress -> done
  - 요약: 평면 충돌을 제거하고 3D 구형 운동을 Canvas 2D에 투영하는 역동적 혼합 연출 완료
  - 산출물: `lotteryMotion.ts`, 투영 단위 테스트, `LotteryMachine.tsx`, README와 SPEC·아키텍처·개발 가이드 갱신
  - 검증: typecheck, 50 tests, build 통과; 데스크톱 45개 혼합 프레임이 620ms 사이 크게 변화, 모바일 가로 넘침·경계 침범·콘솔 오류 없음, 다음 혼합에서 직전 배출 공 재생 없음 확인
  - 리스크_또는_차단: 실제 3D 엔진이 아닌 투영 모델이므로 카메라·조명·물리 정밀도는 제한적
  - 다음: 전체 작업 완료
  - 사용자_피드백: 공 겹침 허용과 3D 구형 투영 기반의 더 역동적인 혼합 요청 반영

- 2026-07-28 15:02 | 단계: T19 | 상태: not_started -> in_progress
  - 요약: 자동 추첨 무작위 간격을 5~10초에서 3~7초로 단축 시작
  - 산출물: 작업 중
  - 검증: 예정 (3초·7초 경계, 누적 시각, 자동 UI 흐름, 회귀 테스트와 빌드)
  - 리스크_또는_차단: 없음
  - 다음: 일정 생성 상수와 관련 테스트·문서 갱신
  - 사용자_피드백: 공 추첨 시간을 3~7초로 조정 요청 반영

- 2026-07-28 15:06 | 단계: T19 | 상태: in_progress -> done
  - 요약: 첫 공을 포함한 모든 자동 추첨 간격을 3~7초의 암호학적 무작위 정수로 단축 완료
  - 산출물: `random.ts`, 자동 일정·엔진·UI 테스트, README와 SPEC·아키텍처·개발 가이드 갱신
  - 검증: typecheck, 50 tests, build, `git diff --check` 통과; 브라우저 자동 추첨 완료와 카운트다운·콘솔 오류 없음 확인
  - 리스크_또는_차단: 없음
  - 다음: 전체 작업 완료
  - 사용자_피드백: 공 추첨 시간을 기존 5~10초에서 3~7초로 조정 요청 반영

- 2026-07-28 15:09 | 단계: T20 | 상태: not_started -> in_progress
  - 요약: 깊이별 크기 차이를 제거하고 중앙을 오가는 3축 혼합 흐름으로 개선 시작
  - 산출물: 작업 중
  - 검증: 예정 (동일 반지름, 중앙 통과, 구형 경계, 회귀 테스트와 데스크톱·모바일 Canvas 시각 검수)
  - 리스크_또는_차단: 중심 힘이 과하면 중앙 군집이 생길 수 있어 회전·횡단·분산 흐름을 함께 검증
  - 다음: 운동·투영 모듈과 단위 테스트 수정
  - 사용자_피드백: 공 크기 통일과 바깥 회전 위주 움직임의 자연스러운 중앙 유입 개선 요청 반영

- 2026-07-28 15:14 | 단계: T20 | 상태: in_progress -> done
  - 요약: 모든 공의 렌더링 크기를 통일하고 공별 위상의 횡단 목표점으로 중앙과 바깥을 오가는 혼합 연출 완료
  - 산출물: `lotteryMotion.ts`, 동일 반지름·중앙 횡단 시뮬레이션 테스트, README와 SPEC·아키텍처·개발 가이드 갱신
  - 검증: typecheck, 52 tests, build, `git diff --check` 통과; 데스크톱·390×844 모바일 45개 혼합에서 동일 크기·중앙/바깥 동시 분포·프레임 변화·가로 넘침 없음과 콘솔 오류 없음 확인
  - 리스크_또는_차단: 실제 공기 흐름을 계산하는 물리 엔진은 아니며 시각적 3축 운동 모델이다.
  - 다음: 전체 작업 완료
  - 사용자_피드백: 공 크기 통일과 중앙을 통과하는 자연스러운 구형 혼합 요청 반영

- 2026-07-28 15:17 | 단계: T21 | 상태: not_started -> in_progress
  - 요약: 일부 추첨 완료 후 남은 공이 중력으로 바닥에 쌓이는 정착 연출 구현 시작
  - 산출물: 작업 중
  - 검증: 예정 (중력 낙하, 구형 경계, 공 간 충돌·감쇠, 회귀 테스트와 데스크톱·모바일 완료 화면)
  - 리스크_또는_차단: 45개에 가까운 잔여 공의 충돌 계산이 모바일 프레임에 미치는 영향을 함께 확인
  - 다음: 완료 상태 prop과 정착 운동 모듈 구현
  - 사용자_피드백: 추첨 완료 후 남은 공이 중력으로 아래에 쌓여야 한다는 요청 반영

- 2026-07-28 15:24 | 단계: T21 | 상태: in_progress -> done
  - 요약: 일부 추첨 완료 후 남은 공에 중력·구형 바닥 반발·공 간 충돌과 마찰을 적용하는 정착 연출 완료
  - 산출물: `App.tsx`, `LotteryMachine.tsx`, `lotteryMotion.ts`, 단일 낙하·44개 적층 테스트, README와 SPEC·아키텍처·개발 가이드 갱신
  - 검증: typecheck, 54 tests, build, `git diff --check` 통과; 데스크톱·390×844 모바일에서 11개 잔여 공의 바닥 적층·감쇠·가로 넘침 없음과 콘솔 오류 없음 확인
  - 리스크_또는_차단: 완료 후 최대 44개에만 3회 충돌 보정을 수행하며 혼합 중 성능과 추첨 로직에는 영향이 없다.
  - 다음: 전체 작업 완료
  - 사용자_피드백: 추첨 완료 후 남은 모든 공이 중력으로 아래에 쌓여야 한다는 요청 반영

- 2026-07-28 18:50 | 단계: T22 | 상태: not_started -> in_progress
  - 요약: 수동 추첨의 첫 버튼 전과 공 사이 대기 구간에도 혼합 애니메이션을 유지하도록 개선 시작
  - 산출물: 작업 중
  - 검증: 예정 (수동 ready/mixing 상시 혼합, 완료 후 중력 정착 전환, 회귀 테스트와 데스크톱·모바일 시각 검수)
  - 리스크_또는_차단: UI 상태 문구와 Canvas 운동 상태의 의미를 혼동하지 않도록 코드와 문서를 분리해 설명
  - 다음: 수동 Canvas 혼합 조건과 상태 테스트 수정
  - 사용자_피드백: 수동 뽑기에서도 공이 계속 섞여야 자연스럽다는 요청 반영

- 2026-07-28 18:52 | 단계: T22 | 상태: in_progress -> done
  - 요약: 수동 추첨의 첫 버튼 전과 공 사이 대기 구간에도 3축 혼합 애니메이션을 계속 유지
  - 산출물: `App.tsx`의 `shouldMixMachine`, 상태 테스트, README와 SPEC·아키텍처·개발 가이드 갱신
  - 검증: typecheck, 55 tests, build, `git diff --check` 통과; 데스크톱·390×844 모바일에서 첫 버튼 전과 첫 공 배출 후 620ms 간격 프레임 변화, 버튼 재활성화, 가로 넘침·콘솔 오류 없음 확인
  - 리스크_또는_차단: 없음
  - 다음: 전체 작업 완료
  - 사용자_피드백: 수동 뽑기에서도 공이 계속 섞여야 자연스럽다는 요청 반영

- 2026-07-28 19:02 | 단계: T23 | 상태: not_started -> in_progress
  - 요약: 추첨 화면의 기본 상단 안내 문구 교체 시작
  - 산출물: 작업 중
  - 검증: 예정 (관련 UI 테스트, 전체 회귀 테스트, 타입 검사와 빌드)
  - 리스크_또는_차단: 없음
  - 다음: `App.tsx`와 테스트 문구 수정
  - 사용자_피드백: `두근두근, 추첨을 시작합니다` 문구 선택 반영

- 2026-07-28 19:02 | 단계: T23 | 상태: in_progress -> done
  - 요약: 추첨 진행 화면의 기본 상단 제목을 `두근두근, 추첨을 시작합니다`로 교체
  - 산출물: `App.tsx`, 관련 `App.test.tsx`, TASK 작업 기록
  - 검증: typecheck, 55 tests, build, `git diff --check` 통과; 이전 문구 잔존 없음 확인
  - 리스크_또는_차단: 없음
  - 다음: 전체 작업 완료
  - 사용자_피드백: 선택한 안내 문구 그대로 반영

- 2026-07-28 19:03 | 단계: T24 | 상태: not_started -> in_progress
  - 요약: 설정 화면의 중복 이름·난수 설명을 사용자 친화적인 소개 문구로 교체 시작
  - 산출물: 작업 중
  - 검증: 예정 (소개 문구 UI 테스트, 전체 회귀 테스트, 타입 검사와 빌드)
  - 리스크_또는_차단: 없음
  - 다음: `App.tsx` 히어로 문구와 테스트 수정
  - 사용자_피드백: 개발자 중심 표현을 사용자 친화적으로 변경 요청 반영

- 2026-07-28 19:03 | 단계: T24 | 상태: in_progress -> done
  - 요약: 중복 이름과 무작위 결과 설명을 친근한 참여·기대감 중심 문구로 교체
  - 산출물: `App.tsx`, 소개 영역 `App.test.tsx`, TASK 작업 기록
  - 검증: typecheck, 55 tests, build, `git diff --check` 통과; 이전 기술 중심 문구 잔존 없음 확인
  - 리스크_또는_차단: 없음
  - 다음: 전체 작업 완료
  - 사용자_피드백: 개발자보다 사용자에게 자연스러운 메시지로 변경 요청 반영

- 2026-07-28 19:04 | 단계: T25 | 상태: not_started -> in_progress
  - 요약: 설정 화면에서 누락된 큰 히어로 제목 교체 시작
  - 산출물: 작업 중
  - 검증: 예정 (히어로 제목·영역 UI 테스트, 전체 회귀 테스트, 타입 검사와 빌드)
  - 리스크_또는_차단: 없음
  - 다음: 설정 화면 `h1`과 테스트 접근 이름 수정
  - 사용자_피드백: 메시지 영역이 변경되지 않았음을 지적하고 `두근두근, 추첨을 시작합니다` 적용 요청

- 2026-07-28 19:05 | 단계: T25 | 상태: in_progress -> done
  - 요약: 설정 화면의 큰 히어로 제목을 `두근두근, / 추첨을 시작합니다` 두 줄로 교체
  - 산출물: `App.tsx` 히어로 제목·접근 이름, 관련 `App.test.tsx`, TASK 작업 기록
  - 검증: typecheck, 55 tests, build, `git diff --check` 통과; 이전 큰 제목의 코드 잔존 없음 확인
  - 리스크_또는_차단: 없음
  - 다음: 전체 작업 완료
  - 사용자_피드백: 누락된 메시지 영역에 요청 문구를 정확히 적용

- 2026-07-28 19:06 | 단계: T26 | 상태: not_started -> in_progress
  - 요약: 설정 화면에서 중복 이름 입력 가능성을 설명하는 문장 제거 시작
  - 산출물: 작업 중
  - 검증: 예정 (소개 영역 UI 테스트, 전체 회귀 테스트, 타입 검사와 빌드)
  - 리스크_또는_차단: 없음
  - 다음: `App.tsx` 본문과 테스트 수정
  - 사용자_피드백: 이름을 여러 개 넣을 수 있다는 의미의 문구는 필요하지 않다는 의견 반영

- 2026-07-28 19:06 | 단계: T26 | 상태: in_progress -> done
  - 요약: 설정 화면 소개 영역에서 중복 이름 설명을 제거하고 기대감 문구 한 문장만 유지
  - 산출물: `App.tsx`, 소개 영역 `App.test.tsx`, TASK 작업 기록
  - 검증: typecheck, 55 tests, build, `git diff --check` 통과; 화면 코드에서 제거 대상 문구 없음 확인
  - 리스크_또는_차단: 없음
  - 다음: 전체 작업 완료
  - 사용자_피드백: 중복 이름 가능성을 굳이 안내하지 않아도 된다는 의견 반영

- 2026-07-28 19:07 | 단계: T27 | 상태: not_started -> in_progress
  - 요약: 설정 화면에 이름과 숫자를 모두 입력할 수 있다는 친근한 안내 문구 추가 시작
  - 산출물: 작업 중
  - 검증: 예정 (소개 영역 UI 테스트, 전체 회귀 테스트, 타입 검사와 빌드)
  - 리스크_또는_차단: 없음
  - 다음: `App.tsx` 본문과 테스트 수정
  - 사용자_피드백: 중복 이름 대신 이름이나 숫자 무엇이든 넣을 수 있다는 의미로 변경 요청

- 2026-07-28 19:08 | 단계: T27 | 상태: in_progress -> done
  - 요약: 설정 화면 소개 첫 줄을 `이름도 숫자도, 원하는 대로 넣어보세요.`로 추가
  - 산출물: `App.tsx`, 소개 영역 `App.test.tsx`, TASK 작업 기록
  - 검증: typecheck, 55 tests, build, `git diff --check` 통과; 새 문구와 기존 기대감 문구 동시 표시 확인
  - 리스크_또는_차단: 없음
  - 다음: 전체 작업 완료
  - 사용자_피드백: 이름이나 숫자 무엇이든 넣을 수 있다는 의미의 안내로 변경 요청 반영

- 2026-07-28 19:12 | 단계: T28 | 상태: not_started -> in_progress
  - 요약: 이름·숫자 중복 공을 `값*반복횟수`로 축약하는 입력 문법 구현 시작
  - 산출물: SPEC 요구사항과 T28 작업 정의
  - 검증: 예정 (파서 단위 테스트, 설정 UI 테스트, typecheck, 전체 테스트, build)
  - 리스크_또는_차단: `*`를 이름 문자로 사용하지 않고 반복식 예약 문자로 정의
  - 다음: 반복식 파싱·검증과 설정 화면 안내 구현
  - 사용자_피드백: `민지*2`, `준호*3` 형식으로 중복 입력을 축약하려는 요청 반영

- 2026-07-28 19:14 | 단계: T28 | 상태: in_progress -> done
  - 요약: 일반 이름·숫자 항목의 `*반복횟수` 확장과 형식·횟수·전체 개수 검증 구현
  - 산출물: 반복식 파서와 설정 화면 안내·테스트, README·SPEC·아키텍처·개발 가이드 갱신
  - 검증: typecheck, 66 tests, build, `git diff --check` 통과; 브라우저에서 `민지*2, 준호*3`의 5개 확장, `민지*0` 차단, `1~45` 회귀와 콘솔 오류 없음 확인
  - 리스크_또는_차단: 없음
  - 다음: 전체 작업 완료
  - 사용자_피드백: 반복 입력 축약 문법 요청을 구현·문서화
