# Agent 가이드라인

## 코딩
- 독립적인 역할을 가진 클래스, 구조체, 모듈, 컴포넌트에는 역할과 책임을 설명하는 주석을 추가한다.
- 코드만으로 구현 목적이나 의사결정 이유가 드러나지 않는 경우, 구현 목적과 의사 결정 과정을 간략하게 요약해서 주석으로 추가한다.
- 변수는 사용되는 위치 가까이에 선언하고, 이름만으로 역할을 이해할 수 있게 작성한다.

### Simplicity
- 코드 작성 및 수정 시 단순한 해결책을 우선한다.
- 새로운 추상화, 계층, 유틸리티, 클래스 추가에는 명확한 근거가 있어야 한다.

## 하위 디렉터리 지침
- 특정 하위 디렉터리의 파일을 작업할 때 해당 경로에 별도 `AGENTS.md`가 있으면 먼저 읽고 따른다.
